---
status: current-remediation-closure
created_at: 2026-08-16
supersedes_scope: "11-测试开发专家全量质量审计与修订计划-2026-08-16.md 的本地可修复项执行状态"
next_lane: validation
---

# 2026-08-16 审计问题修复收口

## 当前结论

审计中所有可以在仓库内通过设计、内容、工件和确定性验证器修复的问题均已落地，并已接入统一重建与发布前门禁。课程的成熟度**没有**因这些修复而升级：真实模型/集成、从业者、学习者和生产验证仍是 `NOT_RUN`，不能称为 `PASS-LIVE`、`PASS-PRACTITIONER` 或 `PASS-PRODUCTION`。

## 问题处置台账

| ID | 处置状态 | 可复用交付物与机器门禁 | 剩余边界 |
| --- | --- | --- | --- |
| P0-01 | 已修复 | `methodology/status-model.schema.json` 将事实、执行、门禁、成熟度和记录生命周期分离；`UNKNOWN` 按 discovery / decision-critical 路由并要求 owner、到期、升级与下一证据。`validate-semantic-contracts.mjs` 对非法传播 fail-closed。 | 真实 Unknown 的关闭仍须由对应 owner 提交证据。 |
| P0-02 | 已修复 | 方法论与源内容只在明确 IID、固定任务分布和重置条件时使用 pass@k / pass^k；其它情形要求逐任务/分片统计及配对或分层 bootstrap CI。 | 指标数值仍须由真实运行数据产生。 |
| P0-03 | 已修复 | 生成源禁止以“95% CI 是否重叠”判定回归；回归判定要求 baseline/current、paired key、effect、CI、非劣界值、slice 和失败原因，语义故障注入会拦截错误表述。 | 真实回归结论仍须有可复现样本与运行收据。 |
| P0-04 | 已修复 | 自动产物改称确定性编辑契约审计；promotion receipt 需要可核验的独立性证明，不再由不同字符串自证独立。 | 当前 103 页均无真实独立评审证明，故全部禁止晋级。 |
| P0-05 | 已产品化，外部执行未开始 | `governance/validation-receipts/` 提供模型/集成、从业者盲评、学习者研究和生产验证收据契约，全部明确为 `NOT_RUN`。 | 必须在独立 validation lane 实际采集 provider/model/参数/输入输出、逐样本评分和人审原始记录。 |
| P1-01 | 已修复 | `validate-evidence-governance.mjs` 强制 TTL、输入内容 hash、校验器 hash、运行起止/退出码和不可变 run record；完整性生成器保留 CI 超时边界。 | 新的内容或评审变更会使旧收据失效，必须重跑。 |
| P1-02 | 已修复 | `human-review/04`–`07` 已标 `superseded`、给出 replacement 与可见横幅；导航仅指向当前审计。 | 历史文档仅供追溯，不可作为现状证据。 |
| P1-03 | 已修复 | 9 条标准均含版本/发布日期/获取日期/固定主工件 hash/生命周期/兼容性/影响范围；`reference-standards-review-queue.json` 和引用验证器阻止静默漂移。 | 当前 5 项进入真实待复核队列，不能由离线检查自动“更新”。 |
| P1-04 | 已修复 | `governance/tevv/ai-tevv-coverage-matrix.yaml` 覆盖 NIST GOVERN/MAP/MEASURE/MANAGE 与模型、检索、工具、应用、人类决策五层；每格含风险、场景、Oracle、数据、owner、证据、监控和回滚。 | 矩阵是测试设计合同，不等于已完成 TEVV。 |
| P1-05 | 已修复 | Judge Card、Gold-set Card 模板要求标注协议、校准、逐类指标、偏差、CI、unknown policy 与暂停条件。 | 不存在真实 gold set / judge 校准收据时，不得发布 Judge 结论。 |
| P1-06 | 已修复 | `courses/learning-paths/` 提供 7/30/60 天路径，每步有前置、输入、工件、检查、常见失败和 capstone。 | 路径有效性仍待目标学员完成率、耗时和错误分布验证。 |
| P1-07 | 已修复 | `courses/prompt-artifact-ownership.json` 声明 learner-one-shot 与 generator-task 的 owner/consumer/不可变文件；生成器对外部 owner fail-closed 并在写前后验 hash。 | 新增工件必须先登记所有权，不能以例外名单绕过。 |
| P2-01 | 已修复 | 架构图的本地 SVG `<img>` 例外已限制范围、补 alt/异步解码并以 `test:architecture-visuals` 回归；lint 为零 warning。 | 构建器仍报告大于 500 kB 的客户端 chunk，需独立性能优化任务处理。 |
| P2-02 | 已修复 | `governance/threshold-policy.yaml` 对阈值定义来源、适用系统/切片、假设、owner、有效期、重标定触发器和 waiver。 | 阈值本身仍须按真实数据与风险容忍度定期重标定。 |
| P2-03 | 已产品化，外部执行未开始 | 学习路径 capstone 与 learner-study receipt 已定义迁移任务、盲评、失败访谈及分层记录要求。 | 学员可用性维持 Unknown，等待真实受试者研究。 |

## 新鲜本地验证证据

2026-08-16 的统一离线重建已通过：引用库（含 9 条标准与 5 条待复核项）→ 14 个内容源/103 页投影 → 语义契约 → TEVV 治理 → 深度、引用、内容、静态投影与类型检查。`site/package.json` 的 `validate:release` 已将语义、TEVV 和证据治理加入发布前链路。

已通过的定向回归包括：语义契约 4/4、TEVV 治理 2/2、证据治理 4/4、完整性生成器 12/12、标准版本契约 5/5、架构图渲染 1/1、技术块 8/8、所有权故障注入 5/5、材料闭包 16 bundles、Prompt 包校验（63 个转换包、52 组三元闭环）。站点 lint 为 0 warning，类型检查通过。

完整性运行记录为 `research/evidence-runs/integrity-2026-08-16T15-50-05-138Z-5b01ec11.json`，记录了输入和校验器 hash、开始/结束时间与退出码。`npm --prefix site run refresh:fixture-evidence` 固化“静态导出 → 收据重签”的顺序，并且只接受本地闭包通过、真实高成熟度仍被阻断的结果；`verify:evidence-governance` 已通过其新鲜度和不可变记录核验。

## 未闭合的晋级阻断

晋级门禁刻意保持 fail-closed：103/103 promotion receipt 为 `FAIL`。740 条 learner-material closure 已全部为 `PASS`；全部 103 页唯一的剩余 finding 是缺少真实、可固定 hash 的独立评审证明。这不是本地验证失败被掩盖，而是防止“自动角色名不同”或 fixture 结果被包装为独立审查/生产有效性的必要阻断。

下一任务必须在 `validation` lane 中执行，且只在真实受控环境、真实独立评审和真实学习者研究各自具备 receipt 后，分别更新对应成熟度；任何失败、拒答、截断、偏差或未闭合项都必须保留在证据中。

# Update log

- 2026-08-09: Rejected previous generic requirement-to-test-case exemplar after user professional review.
- 2026-08-09: Added four-lane AI transformation map, 26-source ledger, 10-offering competitor matrix, 20-course curriculum, and 8 ranked tasks.
- 2026-08-09: Built and ran `td-ai-006-rag-eval-ci` offline lab; preserved PASS→FAIL→PASS reports and evidence.
- 2026-08-10: Split the internal curriculum catalog from the public tutorial. Public HTML, JSON, navigation, search, modules and release promises now contain only 17 validated pages; 48 incomplete catalog topics remain internal. Added fail-closed Skill tests for incomplete pages, empty modules and promise/public-set drift.
- 2026-08-11: Rebuilt the course from the stale 33-page projection into an 85-page local fixture-tested curriculum with per-topic research packages, versioned Prompt/Eval/Mutation assets, typed technical blocks, red-green labs, exact material closure and full-fidelity tutorial projection.
- 2026-08-11: Replaced the fixed shared manuscript-heading gate with a semantic problem/action/repair contract, so topic-specific teaching is accepted while thin or semantically incomplete manuscripts still fail.
- 2026-08-11: Separated local learner visibility from external pilot/public maturity. All 85 pages now map to traced solution units without pretending practitioner review, live integration or publication approval.
- 2026-08-11: Final independent editorial audit reached 85/85 PASS with all evidence boundaries at 100; executability reached 85/85 PASS and 293 typed blocks with zero invalid paths, implicit cwd or legacy-untyped blocks.
- 2026-08-11: Full factory validation with labs, site test/build/lint/typecheck, 13-bundle material closure, tutorial drift check and 390px Playwright sweep passed. No deployment or external publication was performed.
- 2026-08-12: Added a 14-chapter testing methodology handbook (`methodology/`) covering the dimensions the 102 course pages never carried: entry/exit criteria, RACI and decision ownership, defect severity/priority separation, test data and environment management, estimation, a metrics catalog with anti-gaming rules, tool-selection scoring, security/privacy, and a 90-day rollout roadmap.
- 2026-08-12: Built `courses/td-ai-methodology-handbook` as a full course package with 19 templates, 8 checklists, 5 JSON Schemas, a 6-file order-cancellation worked example and a stdlib-only, non-writing self-validator; seven gates with five in-memory fault injections verified at exit codes 0/1/1/1/1/1/0.
- 2026-08-12: Wired the handbook into the TD-M00 lifecycle pages (TD-P01–TD-P08): each page gained a page-specific stage-contract block with a decision table and exit checks, plus five handbook materials. Lifecycle page bodies rose to 2075–3105 CJK characters and 12–15 materials each.
- 2026-08-12: Recorded the measured page-depth deficit (median 1287 CJK characters across 102 pages, minimum 935) and published a per-page enrichment specification with quantified thresholds and a staged gate-raising plan in `methodology/13-逐页扩写规范.md`. The remaining 94 pages are not yet enriched.
- 2026-08-12: Deep-dived TD-M12 (Agent testing architecture) as the exemplar dimension. Mined the frozen user source (8 domains / 36 dimensions / four execution rings / three-stage gate / exchange adapter / migration roadmap) that had only been ~10% projected onto the pages, and published it as 15 dimension documents under `methodology/dimensions/agent-testing-architecture/`.
- 2026-08-12: Replaced the boolean flag checker with `agent_reliability_lab.py`, which actually computes Cohen's kappa, position/verbosity/self-preference probes, pass@k vs pass^k, task-clustered Wilson intervals, first-error distribution, step efficiency, invalid-loop rate, cost percentiles, unit success cost and the three-stage gate. Six gates × eleven in-memory fault injections all verified at 0/1/0; the script writes no files.
- 2026-08-12: Added six computation fixtures, five directly usable templates (Judge Card, reliability Metric Card, blast-radius register, 36-dimension register, four-ring plan), a versioned AG-DIM prompt package (system/task/critic + schema + five-class eval + eight mutations, SHA-256 pinned) and three dimension-level SVGs (36-dimension map, pass@k vs pass^k, three-stage gate).
- 2026-08-12: Enriched all 11 TD-AG pages with four page-specific blocks each (dimension placement, failure-mode-to-method table, failure diagnosis tree, runnable lab). Page bodies rose from ~1030 to 2356–2835 CJK characters and materials from 7 to 11–13; site median rose from 1287 to 1397.
- 2026-08-12: Skill audit found the root cause of thin output. `source-assimilation-and-information-fidelity-contract.md` protects user source → package with atom-level fidelity, but nothing protected research package → learner page. Manuscripts were gated at 1200 characters while delivered pages were gated at `JSON.stringify(page.blocks).length < 750`, which counts JSON syntax. The anti-template rule was a fixed blocklist of literal phrases that any new per-module builder evades, and the editorial score authorizing promotion came from the chain that wrote the page.
- 2026-08-12: Added `references/page-depth-and-projection-fidelity-contract.md` and wired it into SKILL.md (§8A promotion preconditions and a new §9C on dimension document sets). It requires a per-topic projection ledger with zero unaccounted claims, CJK-character depth measurement, measured sentence duplication instead of a phrase blocklist, an independent reviewer id on the promoting score, and staged gate raising.
- 2026-08-12: Implemented the gates in `site/scripts/validate-content.ts`: CJK prose depth (staged 600 baseline / 1250 enriched, target 3000–4000), at least three judgement tables per enriched page, and module-scoped sentence duplication capped at 20% with declared scaffolding exempted via `research/shared-components.json` (30 declared, 0 undeclared).
- 2026-08-12: Retired the dead `buildTopicPage` template — it now throws with an explanation, and `llm-agent.ts` was decoupled from it. A dead template is a defect because the next author picks it up.
- 2026-08-12: Enriched TD-M06 (Benchmark, 6 pages) and TD-M10 (career evolution, 7 pages) with per-page term primers, judgement tables, two counterexamples each, diagnosis trees, drills and takeaways via a shared renderer that writes no teaching prose of its own. Also fixed the duplication introduced by the first TD-M12 pass (41–52% down to under 20%) by parameterising every shared paragraph.
- 2026-08-12: 41 of 102 pages now enriched across five modules (TD-M00, M06, M10, M11, M12). Module minimums: 1733 / 1830 / 1769 / 1717 / 2322 CJK characters; site median rose from 815 to 1028. The remaining eight modules stay at outline density and are excluded from the strengthened gates until enriched.
- 2026-08-12: Repaired three publication-lane defects surfaced while rebuilding the GitHub candidate. Archive filenames were hardcoded to `85p` so a 102-page course still produced a candidate named 85p — the filename itself was a maturity misstatement; they now derive from the current public page count. The solution-architecture coverage table had never been updated after the 85→102 expansion, leaving 17 pages unmapped. Six public-release artifacts (capability profiles, professional evidence, status registry, source assimilation, learner usability, visual sequence) existed under `research/` but were never copied into the candidate root.
- 2026-08-12: Enriched TD-M09 (AI serving, 6 pages) and TD-M04 (Agent/Worker/Workflow, 10 pages). Module minimums rose to 1688 and 1693 CJK characters. Seven of thirteen modules and 57 of 102 pages are now enriched; the site median rose from 1028 to 1730.
- 2026-08-12: Moved every deep-layer content source into the repository at `methodology/dimensions/_sources/*.json` and added two projections from it: `scripts/build-deep-module.py` renders the site TypeScript module, `scripts/build-dimension-docs.mjs` renders 42 Markdown files under `methodology/dimensions/<module>/`. The JSON is the single source of truth, so editing content no longer means editing code, and the same content can now feed a docs site, PDF or offline pack.
- 2026-08-12: Completed enrichment for the final six modules — TD-M07 (5 pages), TD-M02 (4), TD-M01 (7), TD-M03 (8), TD-M05 (8) and TD-M08 (13). All thirteen modules and all 102 pages now carry a term primer, a method judgement table, two counterexamples with "why it looks right", a symptom-to-layer diagnosis tree, a runnable or manual drill, and a page-specific takeaway.
- 2026-08-12: Site-wide measurements after completion — minimum learner prose 1625 CJK characters (was 493), P25 1796, median 1948, total 203,841 characters; every page carries at least three judgement tables. The staged depth gate was raised from 600 to 1600 and the duplication and table gates now apply to all thirteen modules rather than a subset.
- 2026-08-12: Declared 19 further shared scaffolding sentences for TD-M02 in `research/shared-components.json` (49 declared, 0 undeclared). All eleven deep-layer content sources live in `methodology/dimensions/_sources/` and project into 108 Markdown files plus the site modules through two generators.
- 2026-08-14: Measured the whole site and found one external link across 103 pages. `research/source-ledger.csv` held 74 URL-bearing sources and every page already declared `sourceIds`, but nothing rendered them, so research traceability existed while reader-facing citation did not. Added a reference layer: `research/reference-catalog.json` carries only human judgement (what a source proves, what it does not prove, which pages use it) and `scripts/build-reference-library.py` fetches version, licence and last-push from the GitHub API at build time. Version numbers can no longer be hand-written — that was the field most likely to rot unnoticed. 49 entries now cover all thirteen TD-M08 pages; all 127 URLs were verified reachable.
- 2026-08-14: Made licence reuse policy derived rather than declared. `reuse` is computed from the actual SPDX licence: 31 permissive entries are `code-quotable`, 2 weak-copyleft entries are `quote-with-share-alike`, and 16 are `link-only` — k6 (AGPL-3.0), Semgrep (LGPL-2.1) and axe-core (MPL-2.0) were downgraded automatically, and anything GitHub cannot classify falls to the most conservative bucket. The verdict is printed on each reference card so a reader knows whether the code may be copied into their own repository.
- 2026-08-14: Rebuilt the TD-M08 fusion layer. The old `evolution` section was a four-row table that jumped straight from "the traditional practice" to "the fused practice" with no argument in between. Added `evolution.mechanism` (four paragraphs on why the traditional capability works at all, which is what decides whether its premise still holds on AI systems) and `toolchain` (what actually executes each layer and where each tool stops being evidence), hand-written for all thirteen pages. Per-section `refs` render as inline "依据" links and a page-footer source list with version anchors and two-way boundaries.
- 2026-08-14: Restored isomorphism between the two projections. `build-dimension-docs.mjs` had only ever rendered six sections, so failure, evolution, archref, metrics and gate — all present in the JSON — never reached Markdown; each dimension page was 86 lines, half the site, and no gate reported it. Markdown now follows the same order as `deep-layer.ts` and appends the source list; TD-M08 pages are 224–242 lines.
- 2026-08-14: Added `scripts/validate-references.py`, which blocks four regressions: citing an ID absent from the ledger (the renderer discards these silently), stating what a source proves without stating what it does not, a repository citation with no version anchor, and a reuse verdict widened beyond what the licence allows. Fault injection confirmed all four are caught. `--check-urls` adds an optional liveness sweep, off by default so the gate does not depend on the network. `scripts/rebuild-content.sh` now fixes the six-step build order, which was previously scattered across commit messages and silently order-dependent.
- 2026-08-14: Rolled the reference and fusion layers across the remaining twelve content sources — 90 pages. Promoted `mechanism` from inside `evolution` to a top-level optional section: nested, it was only reachable by the traditional-specialisation module; at the top level every module can use the same section to answer why a method works at all, as a migration argument for TD-M08 and as a first-principles argument elsewhere. Extended `DeepSupplement` the same way so the 21 hand-written pages can carry mechanism, toolchain and citations too.
- 2026-08-14: Grew the ledger to 85 entries covering all 103 pages — 61 open-source implementations, 9 standards/RFCs, 8 specifications, 7 official docs; 59 `code-quotable`, 24 `link-only`, 2 `quote-with-share-alike`, all derived from the actual licence. All 227 URLs verified reachable. `build-reference-library.py` now scans the content sources for the `refs` that were actually written and unions them with the catalogue's declared pages: the catalogue records intent, the scan records use, and the two always diverge because nobody goes back to sync them. `validate-references.py` switched to `declaredPages` for the "declared but never cited" check so it cannot degrade into self-confirmation.
- 2026-08-14: Found and fixed a long-standing silent gap. TD-F01 — the site's most-read entry page — had never rendered its supplement. Its failure point, architecture index, metric card and three-stage gate had been sitting in `_sources/requirements-lifecycle-supplement.json` and passing `validate-deep-sources.py` all along, but that page's prose is hand-written in `profession-reality.ts` and never called `requirementsLifecycleSupplement()`. Both content-source gates check whether something was written; neither could see whether it was rendered. Added `site/scripts/validate-reference-projection.ts`, which reads the same `course.ts` and `references.ts` the site renders from and requires every page to have a non-empty source list, at least one body block carrying citation markers, and no unresolvable reference ids. Fault injection confirmed it catches the exact regression. Wired into `validate:release` and `rebuild-content.sh` (now seven steps).
- 2026-08-14: Site-wide result — external links went from 1 across 103 pages to 3–20 per page; 384 reference cards total; body citation markers min 8 / median 8 / max 13 per page; dimension Markdown 197–246 lines per page against 86 before. All fourteen modules are now inside the enforced set for both `validate-deep-sources.py` and `validate-references.py`.

import { EMPTY_DEEP_BLOCKS, renderSupplement, type DeepBlocks, type DeepSupplement } from "./deep-layer.ts";

/**
 * TD-M01「Prompt 小白第一课」深度层。
 *
 * 内容源：methodology/dimensions/_sources/prompt-first-lesson-supplement.json
 * 本文件是投影产物，直接编辑会在下次重建时被覆盖——请改 JSON 后运行
 * `python3 scripts/build-site-modules.py`。
 *
 * 渲染器 `deep-layer.ts` 不写任何教学句：页面上的每一句话都来自 JSON。
 */

const content: Record<string, DeepSupplement> = {
  "TD-FP01": {
    "failure": {
      "title": "把一句指令直接当 Prompt 用，你会得到一个无法复现、无法验收的结果",
      "intro": [
        "一句「帮我生成测试用例」也能出结果，这正是它的危险之处：它能用，但用不了第二次——你说不清这次为什么好、下次为什么差。",
        "下面三条是把 Prompt 当一句话而不是当一个包的后果。"
      ],
      "table": {
        "headers": [
          "失效形态",
          "可核查的数字",
          "它导致的错误结论"
        ],
        "rows": [
          [
            "只保存指令不保存输入与 Schema",
            "复现一次结果至少需要指令、输入、输出 Schema、模型版本 4 类信息，缺任一项复现成功率为 0",
            "「上次效果很好」无法被复现，也无法被改进"
          ],
          [
            "没有输出 Schema",
            "自由文本输出无法被程序消费，人工整理成本随产出量线性上升；生成 100 条的整理耗时可超过自己写 20 条",
            "AI 提效在下游被人工整理成本吃掉"
          ],
          [
            "没有评价规则就判断好坏",
            "无评价规则时「好」由感觉决定；2 个人对同一输出的判断一致率不可估计，换人评审即可能得到相反结论",
            "换个人评审就得到相反结论"
          ]
        ],
        "caption": "本页把 Prompt 从一句话升级成一个包：指令、输入、Schema、评价规则、变异、运行收据。"
      }
    },
    "archref": {
      "title": "这张图的 8 个节点，本页各交付什么",
      "intro": [
        "这张图从业务决策问题出发而不是从「写什么指令」出发。先知道要回答什么问题，才谈得上判断输出好不好。",
        "「Eval·Mutation Oracle」是把这个包变成可验收对象的关键：没有它，Prompt 只是一段祈使句。"
      ],
      "table": {
        "headers": [
          "架构节点",
          "本页负责的部分",
          "出口工件"
        ],
        "rows": [
          [
            "业务决策问题",
            "写清楚这次要回答的业务问题，作为整个包的目的",
            "问题陈述"
          ],
          [
            "System 权限",
            "声明模型可以做什么、不可以做什么",
            "权限与授权边界"
          ],
          [
            "Task 目标",
            "把任务写成可判定的目标，而不是模糊的要求",
            "任务定义"
          ],
          [
            "Input·Context 证据",
            "固定输入与上下文，使结果可复现",
            "输入夹具"
          ],
          [
            "Output·Schema",
            "定义结构化输出，使结果可被程序消费",
            "输出 Schema"
          ],
          [
            "Eval·Mutation Oracle",
            "定义评价规则并用变异证明它有效",
            "评价规则与变异结果"
          ],
          [
            "Manifest·Receipt",
            "记录模型版本与运行收据，使结果可追溯",
            "运行 Manifest"
          ],
          [
            "人工 Owner",
            "指定谁对这个包的产出负责",
            "owner 登记"
          ]
        ],
        "caption": "第一列的节点名与页面架构图逐字一致，由门禁双向校验：引用图上没有的节点、或图上画了却没解释的节点，都会让构建失败。"
      }
    },
    "metrics": {
      "title": "这一页的验收口径：一个 Prompt 包的合格标准",
      "intro": [
        "本页的指标不评价输出质量，评价的是这个包本身完不完整——完整才谈得上稳定，稳定才谈得上改进。",
        "FP01-3 的变异验证是把评价规则从「一段描述」变成「一个会红的检查」的唯一手段。"
      ],
      "table": {
        "headers": [
          "编号",
          "测什么",
          "关键指标",
          "复测频率"
        ],
        "rows": [
          [
            "FP01-1",
            "包完整度",
            "指令 / 输入 / Schema / 评价规则 / 模型版本五项缺失率 = 0",
            "每次使用"
          ],
          [
            "FP01-2",
            "结构化输出",
            "输出可被程序直接消费的比例 = 100%；需人工整理的比例 = 0",
            "每次使用"
          ],
          [
            "FP01-3",
            "评价有效",
            "评价规则的变异杀伤率 ≥ 80%",
            "每次规则变更"
          ],
          [
            "FP01-4",
            "可复现",
            "同一包同一输入重跑得到结构一致输出的比例 = 100%",
            "每次使用"
          ]
        ],
        "caption": "阈值一律是起点不是终点：带来源的按来源引用，其余是结构默认值，必须用你自己前三个版本的实测分布重标定后才能作为验收线。"
      }
    },
    "gate": {
      "title": "这一页的出口判据",
      "intro": [
        "这道门禁很轻，但它是整门课后续所有 AI 使用的前提：一个不完整的 Prompt 包，其产出无法被验收也无法被改进。",
        "第三段要接受的是模型输出的非确定性——包完整不代表输出一定对，它只保证你能判断对不对。"
      ],
      "redline": [
        "包五项要素缺失率 > 0 时阻断使用",
        "输出需人工整理才能被下游消费的比例 > 0 时阻断，须补 Schema"
      ],
      "statistical": [
        "评价规则杀伤率按 95% CI 下界与阈值比较；变异体 < 20 个时结论记为证据不足",
        "输出一致性以多次重跑统计，样本 < 5 次时不下结论"
      ],
      "acceptance": [
        "模型输出的非确定性范围，由使用者知情接受",
        "该 Prompt 包的产出责任人，由团队指定并署名"
      ]
    }
  }
};

export const promptFirstLessonSupplement = (pageId: string): DeepBlocks => {
  const page = content[pageId];
  return page ? renderSupplement(page) : EMPTY_DEEP_BLOCKS;
};

import type { TutorialBlock } from "../course.ts";
import { renderDeepBlocks, type DeepPageContent } from "./deep-layer.ts";

/**
 * TD-M07「专业专题与 Capstone」深度层。
 *
 * 五页此前共用同一组块标题，学习者正文中位 924 中文字符。内容源在
 * methodology/dimensions/_sources/quality-platform.json；请改 JSON 而不是本文件。
 */

const content: Record<string, DeepPageContent> = {
  "TD-QP01": {
    terms: { title: "把需求系统接进测试链的四个词", intro: "Jira 里的一条需求不是测试依据。它要先变成有版本、有来源、有冲突状态的东西。", rows: [
      ["需求事件", "Jira 的创建/变更/状态流转事件；它是触发信号，不是依据本身"],
      ["Basis Gate", "把事件转成冻结依据的那道门；不通过就不允许生成下游工件"],
      ["冲突状态", "同一规则在多处描述不一致时的显式标记；它必须阻断而不是被合并"],
      ["批准权限", "谁有权让候选方案进入执行；这个人不能是生成方案的那个系统"],
    ] },
    method: { title: "从 Jira 事件到可执行方案的五道关卡", intro: ["需求系统的字段是给人看的，不是给程序消费的。中间必须有五道转换，每道都可能停下来。", "右列写的是这道关卡不设时会发生什么——多数是在用例写完之后才暴露。"], table: {
      headers: ["关卡", "检查什么", "不设时的后果"],
      rows: [
        ["事件接入", "事件类型、issue key、变更字段", "把评论变更当成需求变更"],
        ["Basis Gate", "版本、附件、链接文档是否齐全", "依据不全就开始生成用例"],
        ["冲突检测", "描述、验收标准、评论三处是否一致", "模型挑一个最合理的说法"],
        ["候选生成", "AI 只产候选，标注 source_ref", "候选被当成结论直接执行"],
        ["人工批准", "有权限的人显式批准", "没人对这次执行负责"],
      ],
      caption: "五道关卡的顺序不能换。跳过第二道去做第三道，冲突检测会在不完整的依据上给出错误结论。",
    } },
    counter: { title: "两种看起来打通了需求系统、实际不可追溯的做法", intro: ["这两种集成都能跑通，也确实减少了手工搬运。", "问题是产出物无法回答「这条用例依据什么」。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["监听 Jira webhook，需求一变就自动生成用例", "实时性好，减少了人工搬运，链路也清晰", "Jira 的描述字段会被反复编辑。没有冻结版本时，同一条用例昨天和今天依据的文本可能不同，且无法回溯"],
        ["把 issue key 写进用例注释作为追溯", "有 key 就能点回 Jira，追溯关系明确", "issue key 指向的是当前状态，不是生成时的状态。追溯需要的是版本或修改时间戳，不只是 key"],
      ],
      caption: "两条的共同修法：冻结依据时记录版本与内容 hash，而不是只记 ID。",
    } },
    diagnosis: { title: "用例与需求对不上时按什么顺序查", intro: ["「用例和需求不一致」在集成场景里九成是版本问题，不是理解问题。", "按下表逐项查，通常两步内定位。"], table: {
      headers: ["症状", "先怀疑", "下一步"],
      rows: [
        ["用例依据的描述已被改过", "未冻结版本", "检查依据是否记录了内容 hash"],
        ["同一规则两条用例结论相反", "冲突未被检出", "比对描述、验收标准与评论三处"],
        ["用例引用的附件已删除", "附件未快照", "把附件内容一并冻结"],
        ["没人知道谁批准了这次执行", "批准记录缺失", "补批准人、时间与批准范围"],
      ],
      caption: "第四行经常被跳过：自动化跑起来之后，「谁批准的」这个问题往往到出事才被问。",
    } },
    drill: { title: "给一条真实需求做一次 Basis 冻结：约 40 分钟", intro: "不需要接系统。手工走一遍五道关卡，就能看出当前集成缺哪几道。", steps: [
      "选一条最近变更过的真实需求，记录它的 issue key 与当前版本或最后修改时间。",
      "把描述、验收标准、评论、附件四处的内容分别抄出来，各自计算内容 hash。",
      "逐项比对四处对同一规则的描述，把不一致的地方列成冲突清单。",
      "为每条冲突指定升级责任人——不指定就保持 BLOCKED，不进入下游。",
      "最后写下这次执行的批准人与批准范围；写不出来的，说明批准环节尚不存在。",
    ], expected: "典型结果是四处描述里至少有一处不一致，且批准环节从未被显式定义过。这两项都是当天可以立项的修复。", warning: "不要把「以描述字段为准」写成规则来消除冲突。优先级规则必须由产品与技术 owner 共同确认，而不是由集成实现方决定。" },
    takeaway: { title: "带走一份需求接入的五关卡清单", note: ["清单的作用是让「我们接了 Jira」变成「我们在哪五个位置做了什么检查」。", "最容易缺的是第二道和第五道：依据冻结与人工批准。"], bullets: ["冻结依据时记录版本与内容 hash，不只记 ID", "四处描述比对后再进入生成", "批准人与批准范围显式记录"] },
  },
  "TD-QP02": {
    terms: { title: "把证据绑到代码版本的四个词", intro: "流水线里最常见的谎言是：报告是真的，但它属于另一个 commit。", rows: [
      ["SHA 绑定", "每份证据都记录它对应的 commit SHA；没有它证据无法归属"],
      ["JUnit 报告", "标准化的测试结果格式；它只描述结果，不描述覆盖了什么"],
      ["外部质量状态", "来自扫描、评测等外部系统的结论；它常常滞后于当前 commit"],
      ["fail-closed", "证据不完整时阻断而不是放行；这是聚合逻辑的默认行为"],
    ] },
    method: { title: "证据聚合必须回答的五个问题", intro: ["把多个来源的结果聚合成一个结论时，每一份都要能回答自己属于哪个版本。", "右列写的是缺这一项时会出现的具体错误——它们都表现为「明明测过却漏了」。"], table: {
      headers: ["要回答", "靠什么", "缺失时的错误"],
      rows: [
        ["这份报告属于哪个 SHA", "报告元数据里的 commit", "把上次的绿色算成这次的"],
        ["报告是否完整", "期望的 job 数与实际数", "部分 job 没跑却聚合成通过"],
        ["外部状态是否新鲜", "状态生成时间与 SHA", "用几天前的扫描结论放行"],
        ["失败是否被吞掉", "job 退出码与报告解析结果", "解析失败被当成没有失败"],
        ["聚合结论怎么来的", "聚合规则本身", "无法复核结论正确性"],
      ],
      caption: "五个问题都要在聚合器里显式实现。任何一个默认为「有就行」，就会出现属于别的版本的绿色。",
    } },
    counter: { title: "两种看起来在做质量门禁、实际会放过坏版本的做法", intro: ["这两种做法在 MR 界面上都显示绿色对勾，评审时也不会有人怀疑。", "问题是绿色的来源没有被校验。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["Pipeline 全绿即允许合并", "流水线是标准门禁，绿色也确实代表 job 都成功了", "job 成功不等于测试跑了。测试因环境问题跳过、报告为空、或解析失败时，job 仍然可能退出 0。必须断言期望的用例数"],
        ["把外部扫描结论作为 MR 状态展示", "外部系统更专业，结论也更全面", "外部结论常常对应更早的 SHA。必须校验状态里的 commit 与当前一致，否则是在用旧结论放行新代码"],
      ],
      caption: "两条的共同修法：聚合前先校验每份证据的 SHA 与完整性，不满足就 fail-closed。",
    } },
    diagnosis: { title: "合并后立刻出问题时按什么顺序查", intro: ["「门禁是绿的但合并后就坏了」通常不是漏测，是证据属于别的版本或不完整。", "按下表逐项查，四行覆盖绝大多数情况。"], table: {
      headers: ["症状", "先怀疑", "下一步"],
      rows: [
        ["门禁绿但功能坏", "报告属于旧 SHA", "比对报告元数据与合并的 commit"],
        ["某类测试从未失败过", "job 静默跳过", "断言期望用例数而不只看退出码"],
        ["扫描结论一直很好", "外部状态滞后", "校验状态里的 SHA 与生成时间"],
        ["失败被吞掉", "报告解析异常", "解析失败必须视为不通过"],
      ],
      caption: "第二行最隐蔽：一个从不失败的测试集，通常不是质量好，是根本没跑。",
    } },
    drill: { title: "给一次 MR 做证据完整性核对：约 35 分钟", intro: "挑一个最近合并的 MR，把它的绿色拆开看。", steps: [
      "取该 MR 最终通过时的 pipeline，列出所有 job 及其退出码。",
      "打开每份 JUnit 报告，检查里面记录的 commit 与该 MR 的最终 SHA 是否一致。",
      "统计报告里的实际用例数，与预期用例数比较；差异超过一成的说明有 job 静默跳过。",
      "检查外部质量状态对应的 SHA 与生成时间，判断它是否覆盖了本次变更。",
      "把上述四项做成聚合器的断言，任一不满足即 fail-closed。",
    ], expected: "多数团队第一次核对会发现至少一类证据的 SHA 不一致，或用例数明显低于预期。这两项都是可以立刻加进聚合器的断言。", warning: "不要用「重跑一次就好了」处理解析失败。解析失败意味着这次的结论未知，而未知不等于通过。" },
    takeaway: { title: "带走一份证据聚合断言清单", note: ["清单五条，全部围绕同一个问题：这份绿色属于哪个版本、是否完整。", "加进聚合器后，第一周通常会拦下几次此前会被放行的合并。"], bullets: ["每份证据校验 SHA 与生成时间", "断言期望用例数，不只看退出码", "解析失败与证据缺失一律 fail-closed"] },
  },
  "TD-QP03": {
    terms: { title: "临时测试环境的四个词", intro: "每次 MR 起一套环境听起来很贵，但比共享环境的互相污染便宜得多。", rows: [
      ["短生命周期 namespace", "为一次运行创建、用完即销毁的隔离单元"],
      ["ResourceQuota", "限制这套环境能占用的资源上限；防止一次失控拖垮集群"],
      ["NetworkPolicy", "限制它能访问什么；防止测试环境打到生产依赖"],
      ["TTL", "到期自动回收；没有它，被遗忘的环境会持续计费"],
    ] },
    method: { title: "临时环境必须配齐的六项", intro: ["少配任何一项，问题都会在几周后以成本或串扰的形式出现。", "右列写的是缺失时的典型症状——它们往往不会被归因到环境配置上。"], table: {
      headers: ["必配项", "作用", "缺失时的症状"],
      rows: [
        ["独立 namespace", "隔离", "测试互相干扰，结果不可复现"],
        ["RBAC", "最小权限", "测试身份能读到不该读的资源"],
        ["ResourceQuota", "资源上限", "一次失控占满节点"],
        ["NetworkPolicy", "出入方向限制", "测试流量打到生产依赖"],
        ["Job TTL", "自动回收", "被遗忘的环境持续计费"],
        ["审计记录", "可追溯", "出事后查不到谁创建了什么"],
      ],
      caption: "六项都是声明式配置，一次做好后可以模板化。真正的成本在第一次，不在每一次。",
    } },
    counter: { title: "两种看起来在做环境隔离、实际隔不住的做法", intro: ["这两种做法都比共享单一环境进步，也确实减少了一些冲突。", "问题是隔离维度不完整。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["每个 MR 一个 namespace，实现了隔离", "namespace 是 K8s 的标准隔离单元", "namespace 只隔离了 API 对象。没有 NetworkPolicy 时，Pod 仍可访问集群内外的任意地址，包括生产依赖"],
        ["测试结束后在流水线最后一步删除 namespace", "清理逻辑明确，正常路径下也确实删掉了", "流水线被取消或超时时，删除步骤不会执行。必须用 TTL 或独立的回收任务兜底，不能只靠流水线"],
      ],
      caption: "两条的共同要求：隔离要覆盖网络维度，回收要有不依赖流水线的兜底。",
    } },
    diagnosis: { title: "测试结果不可复现时按什么顺序查", intro: ["不可复现的第一嫌疑永远是环境串扰，而不是测试代码。", "按下表逐项排除，前两行占多数。"], table: {
      headers: ["症状", "先怀疑", "下一步"],
      rows: [
        ["单跑通、并行跑挂", "共享了某个资源", "检查是否有集群级共享对象"],
        ["偶尔连到了错误的服务", "缺 NetworkPolicy", "限制出方向并显式声明依赖"],
        ["资源不足导致 Pod 挂起", "无 ResourceQuota", "设配额并让超限快速失败"],
        ["集群里有大量废弃 namespace", "TTL 缺失", "加 TTL 与独立回收任务"],
        ["查不到某环境的来源", "无审计记录", "在创建时打上 MR 与创建者标签"],
      ],
      caption: "第二行最容易被误判成「网络抖动」，实际是测试打到了不该打的地方。",
    } },
    drill: { title: "给临时环境补齐六项配置：约 50 分钟", intro: "先盘点现状，再补最缺的那两项。不需要一次全做。", steps: [
      "列出当前临时环境的创建方式，逐项核对六项配置分别有没有。",
      "优先补 NetworkPolicy：先设默认拒绝出方向，再逐条放通真正需要的依赖。",
      "补 TTL 或独立回收任务，验证在流水线被取消时环境仍会被回收。",
      "在创建时打上标签：MR 编号、创建者、创建时间，供审计使用。",
      "跑一次并行测试，确认两套环境之间没有任何共享资源。",
    ], expected: "补完 NetworkPolicy 后通常会立刻发现有测试在访问未声明的依赖——那些依赖此前一直是隐式的。", warning: "默认拒绝出方向会让一批测试立刻失败。这是预期结果：失败的每一条都对应一个此前未被声明的依赖。" },
    takeaway: { title: "带走一份临时环境配置模板", note: ["模板一次做好可以复用到所有项目。真正的成本在第一次梳理依赖，不在每次创建。", "标签是审计的基础：没有 MR 与创建者标签，环境治理只能靠人记。"], bullets: ["隔离必须覆盖网络维度，不只是 namespace", "回收要有不依赖流水线的兜底", "创建时打标签，供成本与审计追溯"] },
  },
  "TD-QP04": {
    terms: { title: "跨系统事件的四个词", intro: "多系统联动的问题不在单个系统，在它们之间的消息。", rows: [
      ["事件信封", "统一的消息外壳：事件 ID、类型、来源、时间、幂等键"],
      ["幂等键", "保证同一事件被处理多次只产生一次副作用"],
      ["重放", "从历史事件重新推进状态；它是恢复手段也是测试手段"],
      ["脱敏通知", "对外通知中移除敏感字段；通知渠道往往是泄露的最后一公里"],
    ] },
    method: { title: "事件总线必须处理的六种情况", intro: ["事件驱动的系统里，这六种情况都是常态而非异常。不显式处理，它们会在生产以数据不一致的形式出现。", "右列写的是正确行为——注意每一条都要求留下可查的痕迹。"], table: {
      headers: ["情况", "正确行为", "必须留下的痕迹"],
      rows: [
        ["重复投递", "按幂等键跳过", "命中记录"],
        ["乱序到达", "按业务规则决定接受或暂存", "顺序判定依据"],
        ["处理失败", "进重试或死信", "失败原因与次数"],
        ["回写目标不可用", "暂存并重试，不丢事件", "暂存队列深度"],
        ["需要重放", "从指定位点重新推进", "重放范围与执行人"],
        ["对外通知", "脱敏后发送", "脱敏规则版本"],
      ],
      caption: "六行的共同点：都要求事件本身可以被重新处理。做不到重放的系统，故障恢复只能靠人工补数据。",
    } },
    counter: { title: "两种看起来在做事件驱动、实际会丢数据的做法", intro: ["这两种设计在正常流量下完全正常，问题只在故障时暴露。", "而故障时正是最需要它们正确的时候。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["消费失败就记录日志并跳过，避免阻塞后续事件", "不阻塞队列，系统整体可用性更好", "跳过的事件永远丢失了。正确做法是进死信队列并告警，让它可以被重新处理"],
        ["用事件里的业务字段做幂等判断", "不需要额外的键，实现简单", "业务字段可能重复也可能被修改。幂等键必须是事件级的、不可变的标识，且要有明确的作用域与过期时间"],
      ],
      caption: "两条的共同要求：事件必须可恢复、可重放，而不是尽力而为地处理一次。",
    } },
    diagnosis: { title: "跨系统数据对不上时按什么顺序查", intro: ["数据不一致的根因通常在事件处理的某一种边界情况上。按下表逐项排除。", "查之前先确认事件本身是否被完整保存——保存不全时任何排查都是猜。"], table: {
      headers: ["症状", "先怀疑", "下一步"],
      rows: [
        ["数据重复", "幂等键作用域不对", "检查键的构成与过期时间"],
        ["少了一部分数据", "失败事件被跳过", "检查是否有死信队列"],
        ["状态顺序错乱", "乱序未处理", "检查是否有顺序判定逻辑"],
        ["通知里出现敏感字段", "脱敏规则未覆盖新字段", "比对通知模板与数据分类"],
        ["无法恢复到某个时点", "事件未持久化", "确认事件保留期与可重放范围"],
      ],
      caption: "最后一行决定了前四行能不能修：事件没保存，所有恢复手段都不存在。",
    } },
    drill: { title: "做一次事件重放演练：约 55 分钟", intro: "重放是这一层最有价值的能力。演练一次就能知道它到底存不存在。", steps: [
      "选一段历史事件区间，确认它们仍然在保留期内且可读取。",
      "在隔离环境里从该区间起点重放，观察最终状态是否与当时一致。",
      "在重放过程中故意重复投递一条事件，断言副作用只发生一次。",
      "检查重放是否触发了对外通知——多数系统会在重放时误发通知给真实用户。",
      "把重放的范围、执行人、结果写成记录，作为下次故障恢复的操作依据。",
    ], expected: "第四步经常给出意外结果：重放会重新触发通知。发现这一点比完成重放本身更有价值。", warning: "重放演练必须在隔离环境，且通知适配器要显式关闭或指向测试渠道。在生产重放而误发通知，是这类演练最常见的事故。" },
    takeaway: { title: "带走一份事件边界情况清单", note: ["清单六行，每行写明正确行为与必须留下的痕迹。它同时是设计评审和故障复盘的检查表。", "能不能重放，是判断这套事件系统成熟与否的单一最强信号。"], bullets: ["失败事件进死信并告警，不跳过", "幂等键是事件级不可变标识，有明确作用域", "重放演练时关闭对外通知适配器"] },
  },
  "TD-T25": {
    terms: { title: "Capstone 要交付的四个词", intro: "这一页不引入新概念，它检验前面所有工件能不能连成一条链。", rows: [
      ["Release Candidate", "一个可以被评审的完整版本，而不是一堆分散的产物"],
      ["工件链", "从 PRD 到回滚预案的完整链条，每一环都能指向上一环"],
      ["证据闭包", "每个结论都能追到产生它的那次运行"],
      ["成熟度声明", "这份 RC 各项证据分别到了哪一级"],
    ] },
    method: { title: "一个完整 RC 必须包含的十项工件", intro: ["缺任何一项，这个 RC 都无法被独立评审——评审人必须来问你才能理解。", "右列写的是这一项缺失时评审会卡在哪里。"], table: {
      headers: ["工件", "回答什么", "缺失时评审会问"],
      rows: [
        ["需求契约", "要做什么", "这条规则依据什么"],
        ["风险登记册", "为什么测这些", "为什么不测那些"],
        ["Oracle 设计记录", "凭什么判对错", "期望值从哪来"],
        ["数据集与切片", "在什么样本上测的", "样本代表什么"],
        ["Prompt 包", "模型怎么被调用的", "换个说法还成立吗"],
        ["Scorer 与校准", "分数怎么来的", "这个分准吗"],
        ["CI 门禁配置", "什么会被拦下", "坏版本能拦住吗"],
        ["性能与成本", "代价是多少", "高峰扛得住吗"],
        ["Waiver 台账", "哪些没达标", "谁批准的、什么时候过期"],
        ["回滚预案", "出事怎么办", "演练过吗"],
      ],
      caption: "十项里最常缺的是第六与第十：Scorer 校准与回滚演练。它们恰好决定了分数可信度与事故止损能力。",
    } },
    counter: { title: "两种看起来交付完整、实际无法评审的做法", intro: ["这两种交付都包含大量产物，工作量也确实很大。", "问题是产物之间没有连接。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["把所有测试报告、评测结果与文档打包交付", "内容全面，覆盖了各个方面", "报告之间没有引用关系。评审人无法从一个结论追到产生它的输入与版本，只能逐个相信"],
        ["写一份总结文档说明各项都已完成", "有统一视图，阅读成本低", "总结是断言不是证据。每一条断言旁边必须有可打开的产物链接，否则它只是一次自评"],
      ],
      caption: "两条的共同修法：交付的是一张带链接的图，不是一堆文件或一段总结。",
    } },
    diagnosis: { title: "RC 被打回时按什么顺序查", intro: ["评审打回的理由通常可以归到下面四类。每一类都对应一个可以补的具体产物。", "补完之后重新提交，通常不需要重做任何测试。"], table: {
      headers: ["打回理由", "缺什么", "补什么"],
      rows: [
        ["看不出这个结论怎么来的", "证据闭包", "补 run_id 与版本 lineage"],
        ["不知道哪些没测", "残余风险清单", "逐条列出并指定接受人"],
        ["分数不知道准不准", "Scorer 校准", "补 Judge Card 或规则来源"],
        ["出事怎么办没说", "回滚预案", "补演练记录与触发条件"],
      ],
      caption: "四类打回都不是「测得不够」，而是「说不清」。这正是 Capstone 要训练的能力。",
    } },
    drill: { title: "把已有产物组装成一个可评审的 RC：约 3 小时", intro: "不需要新增测试。把前面各页的产物按十项清单归位，缺口自然显现。", steps: [
      "按十项工件清单逐项归位，写下每项对应的文件路径。",
      "为每一项标注它的上游：这份产物依据哪一份产生。没有上游的项要说明原因。",
      "逐条检查证据闭包：随机抽三个结论，看能否追到具体的 run_id 与版本。",
      "写成熟度声明：十项分别处于哪一级，哪些是 fixture-tested，哪些是 NOT_RUN。",
      "请一位没参与过的同事按清单评审，记录他问的每一个问题——那些就是缺口。",
    ], expected: "典型结果是十项里有两三项缺失，且证据闭包在某一环断掉。同事提的问题清单，就是下一版 RC 的工作项。", warning: "不要为了填满清单而临时补写文档。缺失的项如实标为缺失，比补一份没有证据支撑的文档更有价值。" },
    takeaway: { title: "带走一个能被独立评审的 RC", note: ["判断标准只有一个：没参与过的人能不能只看产物就理解并质疑你的结论。", "做不到时，缺的通常不是内容，是产物之间的引用关系。"], bullets: ["十项工件逐项归位并标注上游", "随机抽查证据闭包，验证能追到 run_id", "成熟度逐项声明，缺失如实标记"] },
  },
};

export const qualityPlatformDeepBlocks = (pageId: string): TutorialBlock[] => {
  const page = content[pageId];
  return page ? renderDeepBlocks(page) : [];
};

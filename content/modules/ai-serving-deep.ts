import type { TutorialBlock } from "../course.ts";
import { renderDeepBlocks, type DeepPageContent } from "./deep-layer.ts";

/**
 * TD-M09「AI 接口、性能与可靠性」深度层。
 *
 * 六页此前共用同一组块标题，学习者正文中位 725 中文字符，是全站第二薄的模块。
 * 这里为每页补术语前置、判断表、两个反例、诊断树、演练与带走物；渲染器不写教学句。
 */

const content: Record<string, DeepPageContent> = {
  "TD-A01": {
    terms: { title: "分清普通 API 与生成式 API 的四个词", intro: "两者都是 HTTP，但可测量的对象完全不同。下面四个词决定你会不会用错测试方法。", rows: [
      ["确定性响应", "同一请求同一结果；传统 API 的默认假设，生成式 API 不成立"],
      ["版本化生成服务", "模型、Prompt、参数共同决定行为；接口不变时行为仍可能变"],
      ["能力边界", "服务能做什么与不能做什么；它不写在 OpenAPI 里，只能靠测量"],
      ["单位成功成本", "每个成功完成的调用花费；生成式服务的核心经济指标"],
    ] },
    method: { title: "同一张契约图上的五个测试面", intro: ["普通 REST 只需要测协议与业务。生成式 API 多出三个面，而这三个面通常没人负责。", "右列写的是这一面缺失时会在什么时候暴露——多数是在生产的第一个高峰或第一次模型更新时。"], table: {
      headers: ["测试面", "普通 REST", "生成式 AI API", "缺失时何时暴露"],
      rows: [
        ["协议", "状态码、Schema、错误", "增加流式、工具调用、异步任务", "联调阶段"],
        ["业务", "业务规则正确性", "同上，但无唯一答案", "验收阶段"],
        ["模型行为", "不适用", "非确定性、拒答、截断、长上下文", "上线后第一周"],
        ["算力", "不适用", "显存、批处理、KV Cache、排队", "第一个流量高峰"],
        ["成本", "通常可忽略", "token 长尾、重试放大、评测成本", "第一张账单"],
      ],
      caption: "后三面是生成式服务独有的。把它们交给「后续优化」，等于把最贵的三类问题推到生产暴露。",
    } },
    counter: { title: "两种看起来在测 AI API、实际只测了 HTTP 的做法", intro: ["这两种做法都能产出完整的接口测试报告，覆盖率也很好看。", "它们的共同问题是把生成式服务当成了确定性服务。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["按 OpenAPI 生成契约测试，字段与状态码全覆盖", "契约测试是接口质量的标准做法，自动化程度也高", "OpenAPI 描述不了非确定性、拒答、截断与能力边界。契约全绿的服务可能每三次调用就有一次给出不可用的内容"],
        ["用固定 Prompt 做冒烟，返回 200 且非空即通过", "这是最省事也最稳定的可用性探针", "200 与非空是最弱的判据。模型完全跑题、拒答、或返回被截断的半个 JSON，都会通过这个探针"],
      ],
      caption: "两条的共同修法：把「模型行为」当成一个独立的、必须有人负责的测试面。",
    } },
    diagnosis: { title: "接口测试全绿但用户投诉时按什么顺序查", intro: ["生成式服务的投诉往往落在测试完全没覆盖的三个面上。下表按排查成本从低到高排列。", "前两行不需要压测环境，当天就能查。"], table: {
      headers: ["症状", "先怀疑哪个面", "下一步检查"],
      rows: [
        ["同样的问题有时好有时坏", "模型行为", "同输入重复 5 次，看结果分布"],
        ["回答被截断或答非所问", "模型行为", "断言 finish_reason 与输出完整性"],
        ["高峰期明显变慢", "算力", "看排队深度与 KV Cache 命中"],
        ["账单远超预算", "成本", "看 token 用量 P99 与重试次数"],
        ["模型更新后集体变差", "版本化", "检查是否用了浮动模型别名"],
      ],
      caption: "最后一行是最隐蔽的：接口没改、代码没改、模型别名指向的版本变了。",
    } },
    drill: { title: "给一个在用的 AI API 做五面盘点：约 30 分钟", intro: "不需要工具。把当前的测试用例按五个面分类，看哪几面是空的。", steps: [
      "列出这个 AI API 目前所有的自动化测试用例。",
      "按协议、业务、模型行为、算力、成本五个面给每条分类。",
      "统计每个面的用例数——通常后三面加起来是零。",
      "对空白的面，各写一条最小可行的检查：模型行为写重复运行分布，算力写高峰期分位延迟，成本写 token P99。",
      "确认调用时用的是固定模型版本号还是浮动别名；是别名的，列为待修。",
    ], expected: "盘点结果通常是：协议与业务两面几十条，模型行为、算力、成本三面为零。这张分布图本身就是下个季度的工作计划。", warning: "不要把「重复调用三次都返回 200」当作模型行为测试。它测的还是协议面。" },
    takeaway: { title: "带走一张五面覆盖分布图", note: ["分布图的价值在于它把「我们测得挺全」这个感觉换成了一个可以争论的数字。", "三个空白面各补一条最小检查，比在已覆盖的两面上再加五十条用例有用得多。"], bullets: ["把模型行为、算力、成本列为独立测试面并指定负责人", "调用固定模型版本号，不用浮动别名", "每个面至少有一条最小检查在跑"] },
  },
  "TD-A02": {
    terms: { title: "四种协议形态的关键词", intro: "AI API 的四种交互形态各有各的中途失败方式，最终文本都看不出来。", rows: [
      ["SSE 流", "服务端逐段推送；连接中断与最后一段缺失在最终拼接文本里看不出来"],
      ["结构化输出", "按 Schema 返回；结构合法不等于语义正确"],
      ["工具调用", "模型请求执行外部函数；副作用发生在你的系统里，不在模型里"],
      ["异步任务", "提交后轮询；任务 ID 丢失或状态机漏态会造成静默失败"],
    ] },
    method: { title: "四种形态各自必须断言的状态转移", intro: ["每种形态都有一个状态机。不按状态机测，就只能测到 happy path。", "右列是这一形态最典型的中途失败——它们在最终结果上要么看不出来，要么被当成模型能力问题。"], table: {
      headers: ["形态", "必须断言的状态", "典型中途失败"],
      rows: [
        ["SSE 流", "首包、增量、结束事件、异常终止", "流中断后拼接文本仍然通顺，无法察觉"],
        ["结构化输出", "Schema 校验、必填、枚举、finish_reason", "被截断的 JSON 恰好合法"],
        ["工具调用", "请求、授权、执行、结果回填、失败回退", "授权通过但执行失败，模型继续按成功推理"],
        ["异步任务", "已提交、处理中、成功、失败、超时、取消", "任务丢失后无人发现，用户一直等"],
      ],
      caption: "四行的共同点：最终返回值正常，而中途已经发生了不可接受的事情。",
    } },
    counter: { title: "两种看起来测了流式与工具、实际只测了终态的做法", intro: ["这两种做法在联调阶段都能通过，也确实覆盖了主要路径。", "问题是断言点全在最后。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["把 SSE 流拼接成完整文本后断言内容", "这是用户最终看到的东西，断言它最直接", "流在中途断开时，已收到的部分往往仍然通顺。必须断言结束事件是否收到，以及增量序号是否连续"],
        ["断言工具被调用了且返回 200", "调用发生了、外部系统也返回成功", "要断言的是副作用本身：数据真的写了吗、写了几次、幂等键起作用了吗。工具返回 200 不等于业务状态正确"],
      ],
      caption: "两条都指向同一件事：断言点必须放在中途，不能只放在最后。",
    } },
    diagnosis: { title: "偶发的用户投诉查不到日志时按什么顺序查", intro: ["四种形态的中途失败在常规日志里往往不留痕。下表给出各自的补采集点。", "补齐采集之后，多数「偶发」会立刻变成可复现。"], table: {
      headers: ["症状", "先怀疑", "补什么采集"],
      rows: [
        ["回答突然中断但无报错", "SSE 流异常终止", "记录结束事件与增量序号"],
        ["JSON 解析偶尔失败", "输出被截断", "记录 finish_reason 与输出长度"],
        ["数据写了两次", "工具重试无幂等", "记录幂等键与调用次数"],
        ["用户说提交了没结果", "异步任务丢失", "记录任务 ID 全生命周期状态"],
      ],
      caption: "四行都是采集问题而不是分析问题——采集补齐前，任何分析都是猜。",
    } },
    drill: { title: "给一种协议形态补齐状态断言：约 50 分钟", intro: "挑你系统里用得最多的那一种，先把它的状态机画出来。", steps: [
      "选一种形态（建议从流式或工具调用开始），画出它的完整状态机，含异常分支。",
      "对照现有测试，标出哪些状态转移已经被断言、哪些没有。",
      "为未覆盖的异常分支各写一条测试：流中断、输出截断、工具失败、任务超时。",
      "对工具调用类，追加副作用断言：写入次数、幂等键生效、失败后是否回滚。",
      "跑一遍，确认这些新用例在正常路径上是绿的，在注入对应故障时会变红。",
    ], expected: "典型结果是：现有测试覆盖了状态机的正常路径，异常分支覆盖率接近零。补完之后会立刻抓到一到两个此前不知道的问题。", warning: "异常分支测试必须真的注入异常，不能用 mock 直接返回异常状态。mock 会跳过真正容易出错的那段处理逻辑。" },
    takeaway: { title: "带走四张状态机与它们的异常分支清单", note: ["状态机不需要精美，能标出异常分支即可。它的作用是让「我们测过流式」变成「我们测过流式的哪几种断法」。", "异常分支清单是长期资产：每次协议升级都对照它检查一遍。"], bullets: ["断言点放在中途状态，不只放在最终返回", "工具调用断言副作用本身，不断言返回码", "异常分支必须真的注入，不用 mock 跳过"] },
  },
  "TD-A03": {
    terms: { title: "四个体感指标的准确含义", intro: "这四个词经常被混用，但它们测的是用户体验的不同片段。", rows: [
      ["TTFT", "请求到首 token；用户看到「开始出字」的等待"],
      ["TPOT", "生成阶段每 token 平均间隔；决定出字快慢"],
      ["ITL", "相邻 token 的实际间隔序列；比 TPOT 多了抖动信息"],
      ["Goodput", "满足 SLO 前提下的成功吞吐；容量规划看它，不看裸 QPS"],
    ] },
    method: { title: "从时间戳事件算出四个指标", intro: ["这四个指标不能从网关日志直接得到，必须从 token 级事件时间戳算。缺少事件采集时，任何数字都是估计。", "右列写的是分母——分母不写清楚，两份报告永远对不上。"], table: {
      headers: ["指标", "从哪些事件算", "分母是什么"],
      rows: [
        ["TTFT", "请求发出 → 首个 token 事件", "请求数"],
        ["TPOT", "(末 token 时间 − 首 token 时间) ÷ (token 数 − 1)", "请求数"],
        ["ITL 抖动", "相邻 token 时间差序列的分位", "token 间隔数"],
        ["Goodput", "满足 SLO 的成功请求数 ÷ 时长", "时间"],
        ["单位成功成本", "总花费 ÷ 成功完成数", "成功数，不是请求数"],
      ],
      caption: "最后一行最容易错：用请求数做分母会把失败请求的成本摊薄，掩盖真实浪费。",
    } },
    counter: { title: "两种看起来在测体感、实际测不出体感的做法", intro: ["这两种做法都用了正确的指标名，报表也很规范。", "问题在测量位置与聚合方式。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["用网关的响应时间做 TTFT", "网关是统一入口，采集成本最低", "网关记录的是整个响应完成时间，不是首 token 时间。流式场景下两者可以差几十倍"],
        ["报 TTFT 与 TPOT 的平均值", "均值是最常用的汇总方式，也便于趋势对比", "两者都是长尾分布。平均值既不代表典型用户也不代表最差用户，必须报分位数并按任务类型分桶"],
      ],
      caption: "两条的共同要求：采集要到 token 级，汇总要用分位数。",
    } },
    diagnosis: { title: "指标正常但用户抱怨慢时按什么顺序查", intro: ["体感与指标脱节几乎总是测量位置或分桶问题。下表四行覆盖绝大多数情况。", "查完之后通常会发现：慢的是某一类特定请求，被平均掉了。"], table: {
      headers: ["症状", "先怀疑", "下一步"],
      rows: [
        ["整体分位正常但投诉集中", "未按任务类型分桶", "把长输出任务单独成桶"],
        ["TTFT 好但用户说卡", "ITL 抖动大", "看相邻 token 间隔的分位而不是均值"],
        ["数字与网关日志对不上", "测量位置不同", "统一到 token 事件时间戳"],
        ["吞吐涨了满意度降了", "用了裸 QPS", "改用 Goodput，分母是满足 SLO 的成功数"],
      ],
      caption: "第二行经常被忽略：平均出字速度正常，但每隔几个字卡一下，体感会非常差。",
    } },
    drill: { title: "从一次真实调用的事件流算出四个指标：约 35 分钟", intro: "取一条完整的 token 级事件记录，手工算一遍。算过一次，之后看报表就知道它对不对。", steps: [
      "取一次真实调用的完整事件序列，含请求时刻与每个 token 的到达时刻。",
      "算 TTFT：首 token 时刻减请求时刻。",
      "算 TPOT：末 token 减首 token，除以 token 数减一。",
      "算 ITL 抖动：把相邻间隔排序，取 p50 与 p95，看两者差距。",
      "把这四个数与你们现有监控面板上的对应数字比较，差异超过一成的，说明采集位置不同。",
    ], expected: "手算结果与面板数字往往不一致，差异来源通常是网关时间与 token 事件时间的混用。找出这个差异比新增任何指标都有价值。", warning: "不要用一次调用的结果下结论。这个演练的目的是校准测量方法，不是评估性能。" },
    takeaway: { title: "带走一份写明测量位置与分母的指标定义", note: ["定义要写清三件事：从哪个事件算起、分母是什么、按什么分桶。", "三者一致时，两个团队的报告才能放在一起比较；否则只是两组各自正确的数字。"], bullets: ["采集到 token 级事件，不用网关响应时间", "报分位数并按任务类型分桶", "单位成功成本的分母是成功数，不是请求数"] },
  },
  "TD-A04": {
    terms: { title: "容量测试的四个前提词", intro: "AI Serving 的容量结论极度依赖负载建模。这四个词决定结论能不能用。", rows: [
      ["Token 分布", "输入与输出长度的实际分布；用均值压测会严重高估容量"],
      ["到达率 vs 并发", "前者固定每秒来多少，后者固定同时有多少；两者结论不同"],
      ["缓存命中率", "相同前缀的复用比例；它显著影响容量且容易在压测中失真"],
      ["SLO 拐点", "开始违反 SLO 的负载点；容量指的是它，不是最大吞吐"],
    ] },
    method: { title: "负载模型必须固定的六项", intro: ["压测结论不可复现，九成来自这六项里有几项没被记录。每一项都要写进 workload 文件并打 hash。", "右列是这一项失真时结论会偏向哪一边——全部偏向乐观。"], table: {
      headers: ["必须固定", "为什么", "失真时结论偏向"],
      rows: [
        ["输入 token 分布", "决定 prefill 压力", "偏乐观"],
        ["输出 token 分布", "决定 decode 时长与显存", "严重偏乐观"],
        ["到达模式", "泊松/阶梯/突发的排队行为不同", "偏乐观"],
        ["并发上限", "决定队列深度", "不定"],
        ["缓存命中率", "命中高时算力压力小得多", "严重偏乐观"],
        ["质量切片", "不同任务的失败率不同", "掩盖高风险切片"],
      ],
      caption: "六项里有三项失真会让结论明显偏乐观——这正是压测容量常常是生产容量数倍的原因。",
    } },
    counter: { title: "两种看起来在压容量、实际压出乐观数字的做法", intro: ["这两种做法在压测报告里非常常见，曲线也很漂亮。", "它们的共同结果是把生产容量高估若干倍。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["用一组固定的典型请求反复压测，保证可比", "控制变量、结果稳定、便于跨版本对比", "固定请求会让缓存命中率接近 100%，而生产中命中率低得多。算力压力被系统性低估"],
        ["报最大吞吐作为容量上限", "这是压出来的极限，有数据支撑", "最大吞吐点上延迟往往已远超 SLO。容量应报 SLO 拐点，它通常显著低于最大吞吐"],
      ],
      caption: "两条都需要同一个修正：让压测负载在分布与缓存行为上贴近生产。",
    } },
    diagnosis: { title: "压测容量与生产不符时按什么顺序查", intro: ["按下表逐项对齐，通常前三行就能解释绝大部分差距。", "对齐后重压一次，差距应显著缩小；仍不缩小时才怀疑环境差异。"], table: {
      headers: ["症状", "先怀疑", "下一步"],
      rows: [
        ["压测容量是生产数倍", "缓存命中率失真", "用真实前缀分布重构请求"],
        ["压测无超时生产大量超时", "输出长度按均值", "用 p99 输出长度重压"],
        ["压测延迟平稳生产抖动大", "到达模式过于均匀", "改用泊松或加入突发"],
        ["某类请求在生产特别慢", "未按质量切片分桶", "把该切片单独压"],
      ],
      caption: "第一行的修正成本最低也最有效：仅仅把请求前缀随机化，容量数字通常就会掉一大截。",
    } },
    drill: { title: "用真实分布重建一次容量测试：约 90 分钟", intro: "需要压测环境。若暂无，可先完成前两步的负载建模，标注为未运行。", steps: [
      "从生产采样重建输入与输出 token 的分布，至少取到 p99。",
      "按真实前缀多样性构造请求，避免所有请求共享同一段系统提示导致缓存全命中。",
      "先用闭环固定并发摸出大致范围，再用开环固定到达率找 SLO 拐点。",
      "在拐点附近记录：分位延迟、成功率、Goodput、队列深度、缓存命中率。",
      "把 workload 六项与 SLO 定义一起写进容量卡，并打 hash。",
    ], expected: "用真实分布重压后的容量通常明显低于此前数字。这个下降不是能力退步，是此前的高估被纠正。", warning: "不要在共享推理集群上做拐点测试。邻居负载会污染结果，而且你的压测会影响他们。" },
    takeaway: { title: "带走一张有 workload hash 的容量卡", note: ["容量卡必须写明六项负载参数与 SLO 定义。缺任一项，这个容量数字半年后会被当成系统属性引用。", "报 SLO 拐点而不是最大吞吐——后者对应的延迟通常已经不可用。"], bullets: ["用生产采样重建 token 分布与前缀多样性", "开环找拐点，闭环定位瓶颈", "容量卡写明 workload hash 与 SLO 定义"] },
  },
  "TD-A05": {
    terms: { title: "推理服务的四个阶段词", intro: "「慢」在推理服务里有四个完全不同的位置，修法互不相同。", rows: [
      ["排队", "请求已到达但尚未开始处理；表现为 TTFT 上升而生成速度正常"],
      ["prefill", "处理输入上下文的阶段；受输入长度影响最大"],
      ["decode", "逐 token 生成阶段；受输出长度与批处理影响"],
      ["KV Cache", "缓存注意力键值的显存区域；逐出频繁时 decode 会明显变慢"],
    ] },
    method: { title: "四个阶段各自的信号与修法", intro: ["定位阶段靠信号组合，不靠单一指标。下表给出每个阶段最有区分度的信号。", "右列的修法互不通用——排队问题加显存没用，显存问题调并发也没用。"], table: {
      headers: ["阶段", "最有区分度的信号", "有效修法"],
      rows: [
        ["排队", "队列深度上升、TTFT 上升、TPOT 稳定", "调并发上限或扩容"],
        ["prefill", "TTFT 随输入长度线性上升", "缩短系统提示、启用前缀缓存"],
        ["decode", "TPOT 上升、批大小下降", "调批处理策略"],
        ["KV Cache", "显存接近上限、逐出计数上升", "限制并发长上下文请求"],
        ["下游依赖", "工具 span 耗时上升而模型指标正常", "单独压该依赖"],
      ],
      caption: "五行的判别关键是「TTFT 与 TPOT 谁在动」：只有 TTFT 动是排队或 prefill，TPOT 动是 decode 或显存。",
    } },
    counter: { title: "两种看起来在定位瓶颈、实际在猜的做法", intro: ["这两种做法都会给出一个听起来合理的结论，也常常真的让指标改善了一点。", "问题是结论无法被验证，下次同样的症状还得重新猜。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["延迟高就扩容，扩完确实变快了", "扩容缓解了症状，数据也支持", "扩容对四个阶段都有一点效果，因此它无法证明瓶颈在哪。下次流量再涨还得继续扩，成本线性上升"],
        ["同时调整并发、批大小和超时，整体改善明显", "三项都是合理参数，一起调效率更高", "无法归因也无法回滚。其中某一项可能是负作用，被另外两项掩盖了"],
      ],
      caption: "两条的共同修法：一次只改一层，并记录改动前后的阶段信号。",
    } },
    diagnosis: { title: "服务变慢时的标准排查顺序", intro: ["按下表从上到下走，每一步都能用现有指标回答，不需要新增采集。", "走到哪一行停下来，瓶颈就在那一层。"], table: {
      headers: ["先看什么", "如果是", "结论"],
      rows: [
        ["队列深度是否上升", "是", "排队瓶颈，调并发或扩容"],
        ["TTFT 是否随输入长度上升", "是", "prefill 瓶颈，缩短上下文"],
        ["TPOT 是否上升", "是", "decode 或显存瓶颈"],
        ["显存是否接近上限", "是", "KV Cache 逐出，限长上下文并发"],
        ["模型指标正常但总时长长", "是", "下游依赖瓶颈"],
      ],
      caption: "五行按判别成本排序。前两行用现成指标就能判断，不需要进入推理引擎内部。",
    } },
    drill: { title: "做一次单变量瓶颈验证：约 60 分钟", intro: "先用上表定位到一个候选阶段，然后只改这一层，看拐点是否右移。", steps: [
      "用上表的五步排查定位到一个候选阶段，写下判断依据。",
      "设计一个只影响该阶段的改动：排队改并发上限，prefill 缩短系统提示，decode 调批大小，显存限制长上下文并发。",
      "保持其余全部参数与 workload 不变，重压一次。",
      "比较 SLO 拐点是否右移，以及该阶段的信号是否回落。",
      "若拐点未动，说明判断错误，回到第一步换下一个候选——这也是有效结论。",
    ], expected: "产出是一份带前后信号对比的瓶颈证据。判断错误同样有价值：它排除了一个候选，缩小了范围。", warning: "不要在验证过程中同时更新模型版本或推理引擎版本。那会让前后对比失去意义。" },
    takeaway: { title: "带走一张阶段信号对照表", note: ["对照表的作用是把「服务慢」这个模糊问题变成五个可以逐项回答的判断。", "它同时也是交接文档：下一个值班的人不需要理解全部推理细节就能开始排查。"], bullets: ["先看 TTFT 与 TPOT 谁在动，再往下分", "一次只改一层并记录前后信号", "判断错误也要记录，它缩小了范围"] },
  },
  "TD-A06": {
    terms: { title: "韧性设计的四个词", intro: "AI 服务的失败是常态。这四个词决定失败之后用户看到什么。", rows: [
      ["deadline", "一次请求允许的最长总时间；所有层的超时之和必须小于它"],
      ["重试预算", "一次请求允许的重试总次数，按请求分配而非各层各设"],
      ["Fallback", "主路径不可用时的替代路径，如降级到小模型或缓存"],
      ["Degradation", "主动降低服务能力以保住可用性，如缩短输出、关闭工具"],
    ] },
    method: { title: "按错误类别决定恢复动作", intro: ["不是所有错误都该重试。按类别决定动作，能避免把一次小故障放大成雪崩。", "右列写的是错误做法——其中「全部重试」是最常见也最危险的一种。"], table: {
      headers: ["错误类别", "正确动作", "错误做法"],
      rows: [
        ["限流 429", "按退避重试，计入预算", "立即重试，加剧限流"],
        ["超时", "视 deadline 余量决定是否重试", "无条件重试，累积超过 deadline"],
        ["模型拒答", "不重试，走人工或降级", "反复重试直到「说服」模型"],
        ["输出截断", "可重试但需换参数", "原样重试，大概率再次截断"],
        ["下游 5xx", "有限重试 + 熔断", "无限重试，拖垮下游"],
        ["参数错误 4xx", "不重试，直接失败", "重试，浪费预算"],
      ],
      caption: "六行里只有三类适合重试。把「重试」当成通用容错手段，是延迟与成本失控的主要来源。",
    } },
    counter: { title: "两种看起来在做降级、实际在制造新问题的做法", intro: ["这两种做法都能提高可用性数字，也都在事故复盘中被提出过。", "问题是降级本身没有被测试。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["主模型不可用时自动降级到小模型，保住可用性", "可用性确实提上去了，用户也拿到了回答", "小模型的质量与安全边界不同。降级路径必须有自己的质量下限与安全断言，否则可用性上去了、正确性掉下来且无人知道"],
        ["把降级算作成功，可用性指标从 97% 提到 99.9%", "降级确实返回了可用结果，不算失败", "降级必须在指标里可见。全部算成功时，你会看到一条完美的可用性曲线，同时用户体验在持续下降"],
      ],
      caption: "两条的共同要求：降级是一个独立的、要被测量也要被断言的状态。",
    } },
    diagnosis: { title: "高峰期雪崩后复盘按什么顺序查", intro: ["雪崩几乎总是重试放大加上熔断缺失。按下表逐项核对，每一项都对应一个配置。", "核对完之后通常会发现，最坏调用次数是设计时预期的几十倍。"], table: {
      headers: ["复盘问题", "检查什么", "改进"],
      rows: [
        ["最坏调用次数是多少", "各层重试次数相乘", "预算收到请求级统一分配"],
        ["超时是否逐层递减", "各层超时值", "内层明显小于外层"],
        ["熔断有没有触发", "熔断配置与触发记录", "注入故障验证会触发"],
        ["降级路径试过吗", "降级演练记录", "把演练列入发布准出"],
        ["降级质量有下限吗", "降级路径的断言", "为降级单独设质量门禁"],
      ],
      caption: "第五行最常缺失：降级路径通常只测「能返回」，不测「返回的东西可用」。",
    } },
    drill: { title: "给一条主链路算清重试预算并验证降级：约 50 分钟", intro: "前半是算术，后半需要一次故障注入。两者都不需要压测环境。", steps: [
      "把网关、SDK、应用、模型客户端各层的重试次数与超时值抄下来。",
      "相乘得到最坏调用次数，乘以单次耗时上界，与 deadline 比较。",
      "超出部分即当前配置下必然发生的超时，列为待修。",
      "注入一次主模型不可用，验证 fallback 真的生效、并记录降级后的输出。",
      "对降级输出跑一遍质量与安全断言——多数团队会发现这一层根本没有断言。",
    ], expected: "产出是一张重试预算表加一次降级演练记录。最常见的两个发现是：最坏调用次数远超预期，以及降级路径没有质量门禁。", warning: "降级演练要在有真实流量特征的环境做。用固定测试请求演练，会让降级路径看起来比实际更可靠。" },
    takeaway: { title: "带走一张请求级的韧性预算表", note: ["表要写清每层分到多少次重试、多少秒超时，以及降级后进入哪个状态、该状态的质量下限是什么。", "降级必须在可用性指标里可见——把它算作成功，等于用指标掩盖体验下降。"], bullets: ["按错误类别决定是否重试，不要一律重试", "重试与超时预算在请求级统一分配", "降级是独立状态，要单独设质量与安全断言"] },
  },
};

export const aiServingDeepBlocks = (pageId: string): TutorialBlock[] => {
  const page = content[pageId];
  return page ? renderDeepBlocks(page) : [];
};

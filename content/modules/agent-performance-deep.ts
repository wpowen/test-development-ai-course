import type { TutorialBlock } from "../course.ts";
import { renderDeepBlocks, type DeepPageContent } from "./deep-layer.ts";

/**
 * TD-M11「Agent 性能与稳定性工程」深度层。
 *
 * 八页此前共用同一组块标题与散文，实测学习者正文仅 493–566 中文字符，是全站最薄的模块。
 * 这里为每页补术语前置、判断表、两个反例、诊断树、演练与带走物；所有句子逐页不同，
 * 渲染器 `deep-layer.ts` 本身不写任何教学句。
 */
const content: Record<string, DeepPageContent> = {
  "TD-AP01": {
    terms: { title: "先把 workload 这个词说清楚", intro: "Agent 压测里最贵的错误发生在第一步：把「一次请求」当成压测单位。下面四个词决定了你后面所有数字算在什么对象上。", rows: [
      ["Task", "一次有业务终态的完整工作，可能包含十几次模型调用与工具调用；它才是 Agent 的压测单位"],
      ["Workload 模型", "任务类型、上下文长度、工具路径、允许终态与故障分布的组合；它是版本化工件，不是脚本里的常量"],
      ["允许终态", "一次任务被认为「完成」的所有合法结果，含成功、转人工、安全拒绝；只认成功会把正确的拒绝算成失败"],
      ["入口 2xx", "网关返回成功，但任务可能仍在后台失败；用它代表可用性是最常见的假绿"],
    ] },
    method: { title: "建 workload 时必须分开建模的六个变量", intro: ["压测结论能不能用，取决于 workload 是否还原了真实分布。固定长度、单一任务类型的压测得到的容量数字，在生产上通常高估两到五倍。", "下表左列是必须建模的变量，右列是省略它之后会得到的错误结论——每一条都在真实项目里发生过。"], table: {
      headers: ["必须建模的变量", "省略它会得到什么错误结论"],
      rows: [
        ["任务类型混合比", "只读查询和写操作混算，写操作的失败被大量查询稀释"],
        ["输入 token 分布", "按平均长度压测，p99 长上下文的显存与延迟完全没测到"],
        ["输出 token 分布", "输出长度可变是 Agent 与传统 API 最大的差别；固定输出等于没压"],
        ["工具路径分布", "只压最短路径，多跳路径的累积延迟与失败率不可见"],
        ["允许终态集合", "把「转人工」算作失败，得到一个悲观且无法行动的成功率"],
        ["故障注入比例", "没有故障的压测只能证明晴天可用"],
      ],
      caption: "六个变量都要写进版本化 workload 文件并打 hash；换任一变量即视为换了一次实验。",
    } },
    counter: { title: "两种看起来在压 Agent、实际在压网关的做法", intro: ["下面两种做法都能跑出漂亮的 QPS 曲线，也都能通过评审。它们的问题不在执行，在于压测单位选错了。", "遮住第三列先自己判断——判断不出来的那条，就是你下次压测最可能得到的假结论。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["用固定 Prompt 打入口接口，统计 QPS 与 p99", "这是压测的标准做法，工具现成，数字稳定", "入口返回 2xx 时任务可能还在后台跑。你测的是网关吞吐，不是任务完成能力。真正的分母是 good task 不是 request"],
        ["按平均输入输出长度构造请求，保证可比性", "控制变量、结果可复现，符合实验规范", "Agent 的输出长度是长尾的。按均值压测时 KV Cache 与显存压力完全没出现，容量结论会高估到不可用"],
      ],
      caption: "两条的共同根因是同一个：压测单位从 task 退化成了 request。",
    } },
    diagnosis: { title: "压测数字对不上生产时按什么顺序查", intro: ["压测与生产不一致时，先查 workload 再查系统。多数情况下系统没问题，是实验设计与现实脱节。", "一次只改一个变量，并把改动前后的 workload hash 都记进报告——否则你无法说清是哪一处让结论变了。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["压测容量是生产的三倍", "输入输出 token 分布", "用生产采样重建长度分布再压一次"],
        ["压测无错、生产大量超时", "工具路径分布", "把多跳路径按真实比例加入 workload"],
        ["成功率压测高、生产低", "允许终态定义不一致", "核对「转人工」「安全拒绝」在两边是否都算完成"],
        ["p99 压测好看、用户抱怨慢", "压测单位是 request 不是 task", "改按任务端到端计时，含全部工具与重试"],
        ["每次压测结果差异很大", "workload 未版本化", "给 workload 打 hash，比较两次实验的差异字段"],
      ],
      caption: "五行里有四行的根因在 workload 而不在被测系统——这就是为什么它必须是第一页。",
    } },
    drill: { title: "用一份真实采样重建 workload：约 40 分钟", intro: "这一页不需要压测工具。你需要的是一份生产采样和一张纸——先把分布还原对，压测工具才有意义。", steps: [
      "从生产日志或 trace 里随机抽 200 条真实任务，只抽不改。",
      "逐条记录五项：任务类型、输入 token、输出 token、工具调用次数、最终终态。",
      "算出任务类型混合比、输入输出长度的 p50/p90/p99、工具跳数分布，并列出全部出现过的终态。",
      "把「转人工」「安全拒绝」这类终态单独标出来，明确它们算完成还是算失败，并写下这个判断的 owner。",
      "把上述结果写成一个 workload JSON 并打 sha256；这个 hash 从此进入每一份性能报告。",
    ], expected: "你会得到一份带 hash 的 workload 文件，以及一个通常令人意外的发现：输出长度的 p99 往往是 p50 的数倍，而此前的压测一直按 p50 在跑。", warning: "不要用模型生成「典型任务」来代替采样。生成的分布会向中间集中，恰好抹掉最贵的那条尾巴。" },
    takeaway: { title: "带走一份可以被别人复现的 workload", note: ["workload 文件的价值不在于精确，而在于可被质疑：别人看到 hash 和字段，就能说出你的结论适用于什么、不适用于什么。", "没有 workload hash 的性能数字不可复用——半年后没人能说清那次压测到底压的是什么。"], bullets: ["用 200 条真实采样重建分布，不要用生成数据", "把允许终态写清楚并指定判断 owner", "给 workload 打 hash 并写进每份性能报告"] },
  },
  "TD-AP02": {
    terms: { title: "四个最常被混用的性能词", intro: "Agent 的「慢」有六个来源，而多数报告只报一个总时长。下面四个词决定你能不能把等待拆开。", rows: [
      ["TTFT", "从请求到第一个 token 的时间；它决定流式体验，但在 Agent 里通常发生在若干次工具调用之后"],
      ["TPOT / ITL", "生成阶段每个 token 的平均间隔；总时长 ≈ TTFT + 输出长度 × TPOT"],
      ["Goodput", "满足 SLO 前提下的成功吞吐；裸吞吐上升而 goodput 下降是最常见的容量假象"],
      ["Good task", "一次既完成业务终态、又满足延迟与成本约束的任务；它是 SLI 的分子"],
    ] },
    method: { title: "把用户等待拆成六段，每段有各自的修复手段", intro: ["「慢」不是一个可以修的问题。只有拆成六段之后，才知道该加机器、改并发、还是改重试策略。", "右列写的是这一段变慢时真正有效的动作——注意没有一段的答案是「换更快的模型」。"], table: {
      headers: ["等待来源", "怎么测", "这一段变慢时的有效动作"],
      rows: [
        ["排队", "入队到出队时间", "调并发上限或扩容，不是优化 Prompt"],
        ["首 Token", "出队到首 token", "看 prefill 与上下文长度，缩短系统提示"],
        ["逐 Token", "首 token 到末 token", "看输出长度分布与批处理策略"],
        ["工具调用", "每次 tool attempt 耗时", "超时与依赖治理，与模型无关"],
        ["重试", "重试次数 × 单次耗时", "收紧重试预算；重试是延迟放大器"],
        ["编排", "span 之间的空隙", "查调度与串行化，通常是最被忽略的一段"],
      ],
      caption: "六段之和应当接近端到端时长；差额本身是信号，说明有未被观测的等待。",
    } },
    counter: { title: "两种看起来在优化性能、实际在制造假象的做法", intro: ["这两种做法都能让报表变好看，而且都能给出「改进前后」的对比数据。", "它们的共同问题是：优化的对象不是用户感知的那个量。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["提高并发以提升 QPS，QPS 涨了 40%", "吞吐是标准性能指标，涨了就是变快了", "并发上去后超时与重试增加，goodput 可能反而下降。分母必须是 good task，不是 request"],
        ["把 TTFT 从 2s 优化到 0.8s，体验大幅改善", "首字时间是流式体验的核心指标，优化正确", "如果任务总时长里工具调用占八成，TTFT 优化对用户几乎无感。先拆六段，再决定优化哪一段"],
      ],
      caption: "共同根因：在没有拆分的情况下优化一个局部指标。",
    } },
    diagnosis: { title: "用户说慢但指标正常时按什么顺序查", intro: ["「指标正常但用户抱怨」几乎总是口径问题：测的位置不对，或者分母不对。", "先确认端到端时长是从用户按下按钮算起，而不是从模型调用算起。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["p95 正常但投诉集中", "是否按任务类型分桶", "把长任务单独成桶再看分位"],
        ["吞吐涨了但满意度降了", "goodput 而非裸吞吐", "用 good task 做分子重算"],
        ["六段之和远小于端到端", "编排空隙未被观测", "补 span 之间的时间戳"],
        ["偶发极慢", "重试放大", "统计重试次数分布而不是均值"],
        ["同样负载今天慢明天快", "缓存命中率变化", "把命中率写进每次报告"],
      ],
      caption: "第三行最容易被忽略：观测不到的那段等待，通常就是最长的那段。",
    } },
    drill: { title: "给一条真实任务做一次六段拆解：约 30 分钟", intro: "不需要压测。挑一条端到端偏慢的真实 trace，把它的时间线画出来即可。", steps: [
      "选一条 p90 附近的真实任务 trace，记下端到端总时长。",
      "在时间线上标出六个区间：排队、首 token、逐 token、每次工具调用、每次重试、span 之间的空隙。",
      "六段求和，与端到端总时长比较。差额如果超过一成，说明有未被观测的等待。",
      "找出占比最大的那一段，写下针对它的有效动作（对照上面的表，不要写「优化模型」）。",
      "把同一条任务在低峰再跑一次，比较哪一段变了——变化最大的那段就是与负载相关的瓶颈。",
    ], expected: "你会得到一张六段时间线和一个占比排序。多数团队第一次做完会发现：占比最大的一段不是模型推理。", warning: "不要用平均值做拆解。六段各自都是长尾的，平均之后所有段都会看起来差不多。" },
    takeaway: { title: "带走一本能被别人查的指标字典", note: ["指标字典的作用不是列出指标，而是固定口径：每个指标的分子分母、测量位置、分桶方式各是什么。", "口径不固定时，两次报告之间的涨跌没有意义——你不知道是系统变了还是算法变了。"], bullets: ["每个指标写清分子、分母与测量位置", "goodput 用 good task 做分子，不用 request", "按任务类型分桶后再报分位数"] },
  },
  "TD-AP03": {
    terms: { title: "Trace 相关的四个词", intro: "归因能力完全取决于 trace 的结构。下面四个词决定你能不能从「失败了」走到「第几步坏的」。", rows: [
      ["Task Root", "一次任务的根 span；一条任务对应一个根，是所有归因的挂载点"],
      ["Span 类型", "generation、tool、retry、handoff 四类；类型不分开就无法按类型统计耗时与失败"],
      ["标准字段 vs 本地扩展", "前者跨系统可比，后者承载业务语义；混在一起会让 trace 无法迁移"],
      ["可重放", "凭 trace 就能还原一次运行；不可重放的失败 trace 无法归因，也就无法修"],
    ] },
    method: { title: "一条可用的 trace 必须能回答的五个问题", intro: ["trace 不是日志的另一种写法。它的唯一目的，是让一次失败能被追到具体的那一步。", "如果下面五个问题里有任何一个答不上来，这条 trace 在归因时就是无效的。"], table: {
      headers: ["必须能回答", "靠哪个字段", "缺失的后果"],
      rows: [
        ["这次任务的完整边界在哪", "task root span 的起止", "无法计算端到端时长"],
        ["第几步开始出错", "span 序号 + 状态", "只能知道失败，不能定位"],
        ["调用了哪个工具、什么参数", "tool span 的入参出参", "无法区分工具错误与参数错误"],
        ["重试了几次、为什么", "retry span 与原因码", "把重试成功当成一次成功"],
        ["当时的工具描述是什么", "工具清单快照 hash", "无法证明没被 rug-pull"],
      ],
      caption: "最后一行同时服务于性能归因与安全检测——它是两条链的交点。",
    } },
    counter: { title: "两种看起来 trace 很完整、实际归因不了的设计", intro: ["这两种设计在技术评审里都很难被质疑：字段多、覆盖全、存储也不小。", "问题出在结构而不是数量——trace 的价值来自结构，不来自字段数。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["每次模型调用打一条完整日志，字段有三十多个", "覆盖非常全，出事时有得查", "没有 task root，三十条日志之间没有归属关系。你能看到每次调用，但拼不出一次任务"],
        ["为控制成本，trace 采样率设为 5%", "采样是可观测性的标准做法，成本可控", "高危动作与失败任务恰好是少数。5% 采样意味着大部分需要归因的场景根本没有记录。高危必须 100%"],
      ],
      caption: "第二条的正确做法是分层采样：常规低采样，失败与高危全量。",
    } },
    diagnosis: { title: "拿到失败 trace 却查不下去时按什么顺序查", intro: ["查不下去通常不是分析能力问题，是 trace 结构缺了一环。下表按缺失概率从高到低排列。", "补齐字段的成本远低于反复复现问题的成本——这是可观测性投入最容易算清的一笔账。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["能看到调用看不出任务", "缺 task root", "补根 span 与 trace_id 贯穿"],
        ["知道失败不知道哪步", "span 无序号或无状态", "给每个 span 加序号与终态"],
        ["无法区分工具错与参数错", "tool span 未记入参", "补入参出参，注意脱敏"],
        ["重试被当成一次成功", "retry 未单列 span", "把重试建成独立 span 并记原因"],
        ["无法在测试环境复现", "缺可重放五要素", "补模型版本与种子、完整 prompt、工具快照、时间戳"],
      ],
      caption: "五行的共同点：都不是分析方法的问题，都是采集设计的问题。",
    } },
    drill: { title: "用一条失败 trace 做可重放自检：约 25 分钟", intro: "不需要改代码。拿一条真实失败 trace，逐项对照可重放五要素即可。", steps: [
      "取一条最近的失败任务 trace，不要挑最容易的那条。",
      "逐项检查五要素：模型版本与种子、完整 prompt（含 system）、工具清单快照与 description hash、每次调用的入参出参、时间戳。",
      "缺失的项逐条列出，并标注它属于「采集没做」还是「脱敏时被删掉」——这两种的修法完全不同。",
      "尝试仅凭这条 trace 在测试环境重放一次，记录卡在第几要素上。",
      "把结果写成一条可观测性缺口清单，交给研发 owner，并写明补齐后能多回答哪个问题。",
    ], expected: "多数团队第一次做完会卡在「工具清单快照」这一项上——它是最少被采集、也是唯一能证明工具描述未被篡改的字段。", warning: "脱敏不是删除。把整个入参删掉会让 trace 失去归因价值；正确做法是字段级脱敏并保留结构。" },
    takeaway: { title: "带走一份 Trace Schema 与字段治理表", note: ["治理表要把标准字段与本地扩展分列，并对每个字段标注隐私等级与保留期。", "分不开时会出现两种事故：迁移时本地字段丢失，或敏感字段被长期保留。"], bullets: ["一条任务一个 task root，四类 span 分开", "高危动作与失败任务 100% 记录，不采样", "可重放五要素逐项检查，缺口交给研发 owner"] },
  },
  "TD-AP04": {
    terms: { title: "开环、闭环与协调遗漏", intro: "这一页只讲一件事：你的压测工具在系统变慢时会不会自动降速。会，就测不出真实延迟。", rows: [
      ["闭环负载", "固定并发数，前一个请求完成后才发下一个；系统变慢时发压速率自动下降"],
      ["开环负载", "固定到达率，不管系统是否处理得完；它才能还原真实用户不会因为你慢就少来的事实"],
      ["协调遗漏", "闭环压测在系统变慢时少发了请求，最坏的那批延迟从未被记录"],
      ["计划到达 vs 实际完成", "前者是应当发出的时刻，后者是实际结束的时刻；两者差值才是用户真正的等待"],
    ] },
    method: { title: "同一 workload 在两种负载模型下的差别", intro: ["同一份 workload，用闭环和开环各压一次，得到的结论可以差一个数量级。这不是工具误差，是模型选择。", "右列是两种模型各自能回答的问题——它们不能互相替代，也不能只跑一种。"], table: {
      headers: ["对比项", "闭环（固定并发）", "开环（固定到达率）"],
      rows: [
        ["系统变慢时", "发压自动降速", "到达率不变，队列堆积"],
        ["能回答的问题", "系统在这个并发下的表现", "系统在这个流量下会不会崩"],
        ["延迟统计", "被协调遗漏污染，偏乐观", "接近真实用户感知"],
        ["适合场景", "容量摸底、瓶颈定位", "SLO 验证、容量规划"],
        ["典型误用", "用它得出的 p99 去承诺 SLO", "用它做长稳测试导致成本失控"],
      ],
      caption: "两种都要跑。只跑闭环会低估延迟；只跑开环会难以定位瓶颈。",
    } },
    counter: { title: "两种看起来严谨、实际低估延迟的压测", intro: ["这两种做法都出现在标准压测教程里，也都能产出可复现的报告。", "它们的问题只在一个地方：延迟统计的起点选错了。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["用 100 并发压测，统计每个请求的响应时间", "并发数明确、结果稳定、可复现", "系统变慢时压测工具自动少发了请求，最坏的那批延迟根本没产生。这就是协调遗漏，p99 会被系统性低估"],
        ["延迟从请求发出时刻开始计时", "这是响应时间的标准定义", "应该从「计划到达时刻」开始计。请求在压测工具的发送队列里等了 3 秒，用户是要算进等待的"],
      ],
      caption: "两条都指向同一件事：把压测工具自己的排队算进用户等待。",
    } },
    diagnosis: { title: "压测延迟明显好于生产时按什么顺序查", intro: ["这类差异九成来自负载模型而不是环境差异。先排除协调遗漏，再考虑硬件与网络。", "确认方法很简单：把同一份 workload 用开环重跑一次，看 p99 是否显著上升。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["压测 p99 远好于生产", "闭环协调遗漏", "改开环固定到达率重跑"],
        ["压到某点后延迟不再上升", "发压端已成瓶颈", "检查压测机的 CPU 与连接数"],
        ["延迟分布异常集中", "从发出时刻计时", "改从计划到达时刻计时"],
        ["加并发吞吐不涨", "系统已过拐点", "看队列深度与超时丢弃率"],
        ["两次压测差异大", "到达模式不同", "固定为泊松或阶梯并记录"],
      ],
      caption: "第二行值得单列：压测机本身成为瓶颈时，所有结论都是关于压测机的。",
    } },
    drill: { title: "用同一 workload 跑开闭环对照：约 45 分钟", intro: "不需要新工具。多数压测框架同时支持两种模式，关键是把两次的延迟统计口径对齐。", steps: [
      "取上一页产出的 workload 文件，确认 hash 一致——两次实验必须压同一份负载。",
      "第一轮闭环：固定并发数，记录 p50/p95/p99 与吞吐。",
      "第二轮开环：把第一轮的实际吞吐作为固定到达率，记录同样的分位数。",
      "两轮的延迟统计都改成从「计划到达时刻」开始计，而不是从请求发出时刻。",
      "比较两轮 p99 的差值。差值越大，说明此前的闭环结论被协调遗漏污染得越严重。",
    ], expected: "典型结果是开环 p99 显著高于闭环。这个差值就是你此前的 SLO 承诺被高估的幅度。", warning: "开环压测在系统已过拐点时会迅速堆积，务必设总时长与队列上限，否则会把测试环境压垮。" },
    takeaway: { title: "带走一份开闭环对照报告", note: ["报告的核心不是两个数字，而是它们的差值——那是你的 SLO 承诺此前被高估的幅度。", "此后所有对外的延迟承诺都应基于开环结果，闭环结果只用于内部瓶颈定位。"], bullets: ["同一 workload hash 跑两种负载模型", "延迟从计划到达时刻开始计", "对外承诺用开环，定位瓶颈用闭环"] },
  },
  "TD-AP05": {
    terms: { title: "容量相关的四个词", intro: "「能扛多少」这个问题有四种不同答案，取决于你用哪个量作为容量定义。", rows: [
      ["SLO 拐点", "延迟或成功率开始违反 SLO 的那个负载点；容量指的是它，不是最大吞吐"],
      ["Synthetic goodput", "合成负载下满足 SLO 的成功任务吞吐；它只对这份合成负载成立"],
      ["prefill / decode", "首 token 前的上下文处理阶段与逐 token 生成阶段；两者的瓶颈完全不同"],
      ["阶梯加压", "按台阶逐级提升负载并在每级稳定观察；比连续加压更容易定位拐点"],
    ] },
    method: { title: "五类瓶颈信号与对应的阶段", intro: ["找到拐点只是第一步，真正的产出是「拐点由什么造成」。下表把可观测信号映射到具体阶段。", "一次只改一层，并把改动前后的信号都记下来——否则你无法证明是这一层造成的。"], table: {
      headers: ["信号", "指向哪个阶段", "验证方式"],
      rows: [
        ["队列深度持续增长", "调度与并发上限", "调并发看队列是否回落"],
        ["TTFT 上升而 TPOT 稳定", "prefill / 上下文长度", "缩短系统提示重压"],
        ["TPOT 上升", "decode / 显存与批处理", "看 KV Cache 命中与逐出"],
        ["工具 span 耗时上升", "外部依赖", "单独压该依赖"],
        ["重试次数上升", "超时阈值过紧", "放宽超时看重试是否回落"],
      ],
      caption: "五类信号可以同时出现；此时按队列 → prefill → decode → 工具 → 重试的顺序逐层排除。",
    } },
    counter: { title: "两种看起来找到了容量、实际不能用的结论", intro: ["这两个结论都会被写进容量规划文档，也都能给出压测曲线支撑。", "它们的问题是把一个条件性结论当成了系统属性。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["报告最大吞吐 120 task/min 作为系统容量", "这是压出来的极限值，有曲线为证", "最大吞吐点上延迟可能已经远超 SLO。容量应当报 SLO 拐点，通常显著低于最大吞吐"],
        ["合成负载下 goodput 达标，可支撑上线", "合成负载覆盖了主要任务类型", "goodput 只对这份合成负载成立。必须在报告里写明 workload hash 与适用范围，否则它会被当成系统属性反复引用"],
      ],
      caption: "容量永远是条件性结论：在哪份负载、哪个 SLO、哪个版本下成立。",
    } },
    diagnosis: { title: "加压时找不到清晰拐点按什么顺序查", intro: ["拐点模糊通常说明实验设计有问题：台阶太密、稳定时间太短，或者压测端先到了瓶颈。", "先确认发压端没问题，再怀疑被测系统。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["曲线平滑无拐点", "台阶太密或稳定期太短", "加大台阶间隔，每级至少稳定观察数分钟"],
        ["吞吐早早封顶", "发压端瓶颈", "查压测机 CPU、连接数与出口带宽"],
        ["拐点每次不同", "缓存命中率不一致", "固定缓存状态或记录命中率"],
        ["延迟涨但队列不涨", "单请求变慢", "查 prefill 与上下文长度"],
        ["成功率先掉延迟后涨", "超时阈值触发在前", "把超时与 SLO 阈值对齐后重压"],
      ],
      caption: "第二行最容易被忽略：吞吐封顶时先证明压测机不是瓶颈，否则测的是压测机。",
    } },
    drill: { title: "阶梯加压找拐点并给出瓶颈证据：约 90 分钟", intro: "这是本模块唯一需要真实压测环境的演练。若暂无环境，可以用离线 trace 做归因练习，但必须标注为未运行。", steps: [
      "用固定 workload 与固定 SLO 定义开始，两者都记录 hash 与取值。",
      "按台阶加压：每级提升固定比例，每级稳定观察数分钟后再进下一级。",
      "记录每级的 p95/p99、成功率、goodput、队列深度、TTFT、TPOT、工具耗时与重试次数。",
      "找到第一个违反 SLO 的台阶，把它记为 SLO 拐点——不要报最大吞吐。",
      "对照五类信号表判断瓶颈阶段，然后只改这一层再压一次，验证拐点是否右移。",
    ], expected: "产出是一条容量曲线加一张瓶颈证据卡，卡上必须写明 workload hash、SLO 定义、拐点值与瓶颈阶段。", warning: "不要在共享环境做阶梯加压。它会影响其他团队，而且邻居的负载会污染你的拐点。" },
    takeaway: { title: "带走一张有适用范围的容量卡", note: ["容量卡的关键不是数字，而是数字旁边的限定条件：哪份 workload、哪个 SLO、哪个版本、哪次实验。", "没有限定条件的容量数字会在半年后被当成系统属性引用，然后在一次真实高峰中失效。"], bullets: ["报 SLO 拐点而不是最大吞吐", "把 workload hash 与 SLO 定义写进容量卡", "改一层验证一次，证明瓶颈判断成立"] },
  },
  "TD-AP06": {
    terms: { title: "超时与重试的四个词", intro: "多层重试是 Agent 延迟失控最常见的原因。下面四个词决定你能不能把它算清楚。", rows: [
      ["总 deadline", "一次任务允许的最长时间；所有层的超时之和必须小于它，否则外层永远等不到内层结束"],
      ["重试预算", "一次任务允许的重试总次数；它是任务级的，不是每层各自的"],
      ["重试放大", "网关、SDK、Agent、Tool 各自重试三次，最坏情况是 81 次调用"],
      ["安全降级", "超预算时的可接受终态，如只读结果、转人工、明确拒绝；它不是失败"],
    ] },
    method: { title: "四层重试如果各自独立，最坏会放大多少", intro: ["每一层的重试策略单独看都合理，叠加之后就成了乘法。这不是假设——这是重试相关事故的标准剧本。", "右列写的是把它算清楚之后的正确做法：预算在任务级统一分配，不在各层各自设定。"], table: {
      headers: ["层", "单层重试", "独立设定的后果", "正确做法"],
      rows: [
        ["网关", "3 次", "× 3", "只做一次，或不做"],
        ["SDK", "3 次", "× 9", "关闭或纳入总预算"],
        ["Agent", "3 次", "× 27", "从任务级预算中扣减"],
        ["Tool", "3 次", "× 81", "按依赖幂等性单独授权"],
      ],
      caption: "81 这个数字不是极端假设，它是四层各自「合理」设置三次的直接结果。",
    } },
    counter: { title: "两种看起来在提高可靠性、实际在制造事故的配置", intro: ["这两种配置都能在短期内让成功率上升，也都很难在评审中被否决。", "它们的代价要到一次高峰或一次依赖抖动时才会显现。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["每层都加重试，成功率从 92% 提到 98%", "重试是标准容错手段，数字也确实改善了", "四层独立重试最坏放大 81 倍。依赖抖动时它会把一次小故障放大成雪崩，而且非幂等工具会产生重复副作用"],
        ["超时统一设为 30 秒，简单一致", "统一配置便于维护，也留足了余量", "外层 30 秒、内层也 30 秒时，外层永远等不到内层的超时结果。超时必须逐层递减，且总和小于任务 deadline"],
      ],
      caption: "两条的共同要求是同一件事：把超时与重试当成任务级预算来分配。",
    } },
    diagnosis: { title: "压力下延迟或副作用异常时按什么顺序查", intro: ["重试相关的故障有一个特征：平时完全正常，一旦依赖变慢就急剧恶化。", "查的时候先算最坏放大倍数，再看幂等保护。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["依赖稍慢就雪崩", "多层重试放大", "算最坏倍数，把预算收到任务级"],
        ["出现重复副作用", "非幂等工具被重试", "检查幂等键与重试授权"],
        ["外层超时从不触发", "超时未逐层递减", "让内层超时明显小于外层"],
        ["降级后用户看到报错", "降级终态未定义", "把只读/转人工/拒绝定义为合法终态"],
        ["成功率高但成本翻倍", "重试吃掉了预算", "把重试成本单列进成本报告"],
      ],
      caption: "第四行是体验问题也是数据问题：降级被算成失败时，SLI 会低估系统的真实可用性。",
    } },
    drill: { title: "给一条任务链算一次重试预算：约 35 分钟", intro: "这一步是算术，不是压测。把四层配置抄下来相乘，多数团队会在这一步发现问题。", steps: [
      "把网关、SDK、Agent、Tool 四层的重试次数与超时值分别抄下来，注明配置在哪里。",
      "相乘得到最坏调用次数，再乘以单次耗时上界，得到最坏总时长。",
      "把最坏总时长与任务 deadline 比较。超出的部分就是当前配置下必然发生的超时。",
      "逐层检查超时是否递减，且总和小于 deadline；不满足的层列为待修。",
      "为每个可写工具确认幂等键存在，并明确它是否允许被自动重试；不允许的必须在重试层排除。",
    ], expected: "多数团队第一次算完会发现最坏调用次数在两位数以上，且至少有一层的超时没有递减。这两项都是可以当天提交的修复。", warning: "不要用「实际上很少同时重试」来跳过这个计算。重试放大恰恰在依赖抖动时同时发生，那正是你最需要它不发生的时刻。" },
    takeaway: { title: "带走一张任务级的超时与重试预算表", note: ["预算表要写清每一层分到多少次重试、多少秒超时，以及超预算后进入哪个降级终态。", "把降级终态写清楚同样重要——否则安全降级会被 SLI 算成失败，团队就会去掉降级。"], bullets: ["重试预算在任务级统一分配，不在各层各设", "超时逐层递减且总和小于 deadline", "把只读、转人工、拒绝定义为合法终态"] },
  },
  "TD-AP07": {
    terms: { title: "长稳测试的四个词", intro: "短压测能发现容量问题，发现不了泄漏。下面四个词用来区分「正常升温」与「真的在漏」。", rows: [
      ["资源斜率", "内存、连接、句柄随时间的增长斜率；持续正斜率且不回落才是泄漏"],
      ["平台期", "资源上升后稳定的水平；有界缓存会形成平台期，泄漏不会"],
      ["快照差分", "两个时间点的对象或连接快照相减；它比曲线更能定位泄漏来源"],
      ["cleanup 成功率", "会话、临时环境、连接的回收成功比例；它下降通常早于内存曲线异常"],
    ] },
    method: { title: "四种曲线形状分别意味着什么", intro: ["长稳测试的判读是形状判断，不是阈值判断。同一条上升曲线，可能是冷启动、缓存热身、有界缓存，也可能是泄漏。", "右列写的是确认方式——只看曲线无法区分，必须配合快照差分。"], table: {
      headers: ["曲线形状", "可能含义", "确认方式"],
      rows: [
        ["快速上升后回落", "冷启动", "与首次启动时间对齐"],
        ["缓慢上升后形成平台", "缓存热身或有界缓存", "查缓存上限配置"],
        ["持续上升不回落", "泄漏", "两点快照差分，看增长最多的对象"],
        ["锯齿状上升", "回收不及时", "查 cleanup 成功率与回收周期"],
      ],
      caption: "第三行是唯一必须立刻处理的一种；其余三种需要记录但不一定是缺陷。",
    } },
    counter: { title: "两种看起来做了长稳、实际测不出泄漏的做法", intro: ["这两种做法都占用了真实的机器时间，报告也确实写了「长稳通过」。", "问题在于测试设计让泄漏没有机会显现。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["跑 8 小时长稳，内存平稳，判定无泄漏", "时长足够，曲线平稳，结论有据", "如果负载很低，泄漏速率会低于噪声。长稳要在接近拐点的负载下跑，而不是在空闲负载下跑"],
        ["每小时重启一次以保证稳定性", "重启确实让服务更稳定，是常见运维手段", "定期重启会掩盖泄漏。长稳测试期间必须禁止自动重启，否则测的是重启周期不是稳定性"],
      ],
      caption: "两条的共同点：让被测对象没有机会表现出问题。",
    } },
    diagnosis: { title: "长稳期间指标漂移时按什么顺序查", intro: ["长稳测试的价值在于慢变量，所以判读要看趋势不看瞬时值。先排除环境因素，再怀疑代码。", "每次判读都同时看三条线：资源、尾延迟、cleanup 成功率。"], table: {
      headers: ["症状", "先怀疑", "下一步检查"],
      rows: [
        ["内存持续上升", "泄漏或无界缓存", "两点快照差分，看增长最多的对象类型"],
        ["尾延迟缓慢变差", "GC 压力或连接堆积", "看 GC 频率与连接池使用率"],
        ["cleanup 成功率下降", "回收逻辑异常", "查失败回收的具体资源类型"],
        ["成功率周期性波动", "定时任务干扰", "把定时任务时间与波动对齐"],
        ["前几小时正常后突变", "缓存达到上限", "查上限配置与逐出策略"],
      ],
      caption: "cleanup 成功率下降通常早于内存曲线异常，是最有价值的先行指标。",
    } },
    drill: { title: "设计一次能测出泄漏的长稳：约 20 分钟设计 + 数小时运行", intro: "设计比运行重要。多数长稳测不出问题，是因为负载太低或允许了重启。", steps: [
      "把负载设在 SLO 拐点的七成左右——太低测不出泄漏，太高会因过载掩盖趋势。",
      "关闭测试期间的自动重启、自动扩缩容与定时清理，否则测的是这些机制。",
      "在开始、中段、结束各取一次资源快照（内存对象、连接、句柄、临时文件）。",
      "全程记录资源曲线、尾延迟趋势与 cleanup 成功率三条线。",
      "结束后做首尾快照差分，按增长量排序，列出前若干类对象并逐个判断是否有界。",
    ], expected: "产出是三条趋势线加一份快照差分排序。若差分排在最前的对象类型没有明确上限，那就是泄漏候选。", warning: "不要在长稳期间部署新版本。中途变更会让首尾快照差分失去意义，整轮测试作废。" },
    takeaway: { title: "带走一份长稳趋势与差分报告", note: ["报告要同时给出曲线形状判断与快照差分证据。只有曲线的报告无法区分有界缓存与泄漏。", "把负载水平写在报告首行——它决定了这份结论的适用范围。"], bullets: ["负载设在拐点七成，禁用重启与自动清理", "三条线一起看：资源、尾延迟、cleanup 成功率", "首尾快照差分排序，判断增长对象是否有界"] },
  },
  "TD-AP08": {
    terms: { title: "SLO 与告警的四个词", intro: "性能工作的最后一步是让数字变成可执行的响应。下面四个词决定告警是有用还是被静音。", rows: [
      ["SLI", "服务质量指标；Agent 场景应以 good task 为分子，而不是 HTTP 成功率"],
      ["错误预算", "允许违反 SLO 的额度；它把「要不要发布」从争论变成算术"],
      ["Burn rate", "错误预算的消耗速率；多窗口组合可以同时兼顾快响应与低误报"],
      ["止血 vs 修复", "先恢复用户可用性，再定位根因；两者的 owner 与时限不同"],
    ] },
    method: { title: "多窗口 burn-rate 告警怎么配才不会被静音", intro: ["单窗口告警只能二选一：要么漏报慢烧，要么在抖动时狂响。多窗口组合是为了同时避免这两件事。", "右列写的是每条告警对应的响应动作——没有动作的告警最终一定会被静音。"], table: {
      headers: ["窗口组合", "捕捉什么", "响应动作"],
      rows: [
        ["短窗 + 长窗，高倍率", "突发严重故障", "立即止血，考虑回滚"],
        ["中窗 + 长窗，中倍率", "持续劣化", "当班排查，准备降级"],
        ["长窗，低倍率", "缓慢消耗", "排期修复，不打断当班"],
        ["预算耗尽", "本周期已超支", "冻结高风险发布"],
      ],
      caption: "每一行都必须有具名 owner 与时限；没有 owner 的告警在第三次响起后就会被关掉。",
    } },
    counter: { title: "两种看起来监控完善、实际不响或响个不停的配置", intro: ["这两种配置在监控评审时都很难被质疑：一个覆盖全，一个响应快。", "它们的失效方式相反，但根因相同——SLI 的分子选错了。"], table: {
      headers: ["看起来合理的做法", "为什么它看起来是对的", "它实际上漏掉了什么"],
      rows: [
        ["用 HTTP 5xx 率作为 SLI，超过阈值告警", "这是最标准的可用性指标，工具原生支持", "Agent 任务失败时接口往往返回 200。用 5xx 做 SLI 会让最典型的失败完全不告警。分子必须是 good task"],
        ["把告警阈值调低以便早发现问题", "早发现是监控的目的，调低更敏感", "低阈值在正常抖动时频繁触发。三次误报之后告警会被静音，然后真故障也不会响。用多窗口 burn-rate 替代单一低阈值"],
      ],
      caption: "告警的可用性不取决于灵敏度，取决于每次响起时人知道该做什么。",
    } },
    diagnosis: { title: "事故复盘时按什么顺序检查告警链", intro: ["复盘的目的不是追责，是找出这次为什么没有被更早发现。按下表逐项走一遍，每一项都对应一个具体改进。", "把改进写成回归资产——性能事故也要走 Trace-to-Regression。"], table: {
      headers: ["复盘问题", "检查什么", "改进方向"],
      rows: [
        ["为什么没告警", "SLI 分子是否为 good task", "改用任务级 SLI"],
        ["为什么告警晚", "窗口组合是否缺短窗", "补短窗高倍率规则"],
        ["为什么没人响应", "是否有具名 owner 与时限", "补 owner 与升级路径"],
        ["为什么止血慢", "降级与回滚是否演练过", "把演练列入发布准出"],
        ["为什么复发", "是否补了回归用例", "把事故样例回流为固定回归"],
      ],
      caption: "第五行最容易被跳过：性能事故往往只修配置不补回归，于是半年后同样的高峰再来一次。",
    } },
    drill: { title: "给一次真实事故补完告警链：约 40 分钟", intro: "挑一次最近的性能事故，按复盘表逐项走一遍。不需要工具，需要的是事故当时的时间线。", steps: [
      "还原时间线：用户首次受影响、监控首次异常、告警首次触发、人首次响应、止血完成、恢复完成，各记时刻。",
      "计算三个差值：受影响到告警、告警到响应、响应到止血。哪一个最大，改进就先做哪一个。",
      "检查当时的 SLI 分子是不是 good task。如果是 HTTP 成功率，这就是「为什么没告警」的答案。",
      "为每条告警补上具名 owner、响应时限与升级路径，并删掉没有响应动作的告警。",
      "把这次事故的输入与版本做成一条固定回归用例，并在下一次发布前跑一遍。",
    ], expected: "产出是一份带三个时间差的复盘表加一条新增回归用例。三个差值里最大的那个，就是下个季度最值得投入的方向。", warning: "不要在复盘中把「监控没覆盖到」当作结论。它是症状；结论应当是具体哪个 SLI 的分子选错了，或者哪个窗口缺失。" },
    takeaway: { title: "带走一份 SLO 与事故 Runbook", note: ["Runbook 的价值在凌晨三点体现：当班的人能不能在不理解全系统的情况下完成止血。", "所以每条告警旁边必须写清第一个动作是什么，而不是写它意味着什么。"], bullets: ["SLI 用 good task 做分子，不用 HTTP 成功率", "多窗口 burn-rate 替代单一低阈值", "每条告警有具名 owner、时限与第一个动作"] },
  },
};

export const agentPerformanceDeepBlocks = (pageId: string): TutorialBlock[] => {
  const page = content[pageId];
  return page ? renderDeepBlocks(page) : [];
};
